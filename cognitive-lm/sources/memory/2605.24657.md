https://arxiv.org/abs/2605.24657
Beyond Inference-Only Deployment: Comparing Weight-Based Consolidation Against Cascading Compaction
Authors: Simon Dennis, Kevin Shabahang, Hao Guo, Rivaan Patil
Year (first arXiv version): 2026
arXiv comment: 15 pages
PDF: pdf/2605.24657.pdf (downloaded 2026-09-26; text extracted with pdftotext)

## Abstract (arXiv API)

Major LLM platforms deploy models in an inference-only configuration: the model serves requests but never updates per-user weights. Users must repeatedly re-teach preferences, corrections, and project context, and context-based workarounds consume context-window space and degrade under cascading compaction. We evaluate an alternative: nightly consolidation of interaction knowledge into model weights via reflection, synthesis, and Low-Rank Adaptation (LoRA) fine-tuning on a single consumer GPU. Across ten realistic software development conversations (n = 10, 1,146 test questions across three memory types), three cycles of cascading compaction retain 36.8 +/- 3.0% of knowledge (between an 11.8% no-context floor and a 90.1% full-context ceiling), while consolidation retains 80.4 +/- 1.3% -- a 43.6 pp gain (paired t(9) = 14.8, p < 0.001) that more than doubles what compaction preserves, with the largest gains on procedural corrections (36.3% -> 74.6%) and episodic project facts (31.5% -> 78.2%). As a methodological aside, mean per-token validation cross-entropy is negatively correlated with LLM-judged accuracy (r = -0.51) while median per-token validation cross-entropy tracks accuracy almost exactly (r = +0.99): under evaluators that tolerate surface-form variation, the mean is misleading and a heavy-tail-robust statistic is the faithful signal. Persistent personalization requires moving beyond inference-only deployment toward architectures that consolidate knowledge into weights.

## Full text

--- Page 1 ---
arXiv:2605.24657v1 [cs.AI] 23 May 2026

Beyond Inference-Only Deployment:
Comparing Weight-Based Consolidation Against
Cascading Compaction

Simon Dennis
i14 / University of Melbourne

Kevin Shabahang
i14

Hao Guo
i14

Rivaan Patil
i14

Abstract
Major LLM platforms deploy models in an inference-only configuration: the model
serves requests but never updates per-user weights. Users must repeatedly reteach preferences, corrections, and project context, and context-based workarounds
consume context-window space and degrade under cascading compaction.
We evaluate an alternative: nightly consolidation of interaction knowledge into
model weights via reflection, synthesis, and Low-Rank Adaptation (LoRA) finetuning on a single consumer GPU. Across ten realistic software development
conversations (n = 10, 1,146 test questions across three memory types), three
cycles of cascading compaction retain 36.8 ± 3.0% of knowledge (between an
11.8% no-context floor and a 90.1% full-context ceiling), while consolidation
retains 80.4 ± 1.3%—a 43.6 pp gain (paired t(9) = 14.8, p < 0.001) that more
than doubles what compaction preserves, with the largest gains on procedural
corrections (36.3% → 74.6%) and episodic project facts (31.5% → 78.2%). As a
methodological aside, mean per-token validation cross-entropy is negatively correlated with LLM-judged accuracy (r = −0.51) while median per-token validation
cross-entropy tracks accuracy almost exactly (r = +0.99): under evaluators that
tolerate surface-form variation, the mean is misleading and a heavy-tail-robust
statistic is the faithful signal.
Persistent personalization requires moving beyond inference-only deployment
toward architectures that consolidate knowledge into weights.

1

Introduction

Large language models have transformed how we interact with AI systems, but they share a fundamental limitation: once deployed, they do not learn from their interactions with individual users.
Consider a software developer who uses an LLM assistant daily. Over weeks of collaboration, they
repeatedly tell the assistant to use snake_case for function names, to prefer pathlib over os.path, and
that their project uses a specific technology stack. Despite these repeated instructions, nothing is
transferred to the model’s weights. Some platforms carry forward summaries or compacted context
between sessions, but this information competes for finite context window space and degrades under
repeated compaction—it is a workaround, not learning.
The practical consequences are significant. Corrections and preferences do not accumulate—the
model makes the same mistakes in new conversations and cannot adapt to individual users. Most
critically, over months of collaboration, the volume of relevant project context far exceeds what any
context window can hold. Architecture decisions, past debugging sessions, team conventions, and
user preferences accumulate continuously, but the context window does not grow to match. Contextbased workarounds—retrieval-augmented generation [Lewis et al., 2020] and external memory stores
[Zhong et al., 2024, Chhikara et al., 2025]—can defer this problem but cannot solve it.
Preprint.


--- Page 2 ---
Biological brains face a similar challenge, and Complementary Learning Systems (CLS) theory
[McClelland et al., 1995] offers a useful analogy. The brain uses a fast system (hippocampus) for
rapid encoding of new experiences and a slow system (neocortex) for persistent knowledge, with
consolidation transferring information from the fast to the slow system [Kumaran et al., 2016, Sun
et al., 2023]. In LLMs, attention over the context window plays the fast-encoding role while model
weights serve as persistent storage. The analogy is particularly strong for linear attention models,
which implement Hebbian learning similar to that proposed for hippocampal area CA3 [Rolls, 2013].
Fine-tuning a model on its own interaction data is well-studied in continual learning [Kirkpatrick
et al., 2017, Parisi et al., 2019], and recent work has begun to explore weight-based personalization at
deployment time [Tan et al., 2024]; nonetheless, production assistants overwhelmingly leave user
weights frozen and rely on context-based memory.
We propose a three-stage consolidation pipeline—reflection, synthesis, training (Figure 1, §2.2)—
built around two design choices: LoRA-based architectural isolation [Hu et al., 2021], which lets
new information be assimilated into the model’s weights quickly while keeping the base model’s
pre-trained capabilities intact, and the use of an off-the-shelf pretrained LLM as the synthetic-data
generator—a pattern established by Self-Instruct [Wang et al., 2023b]. The rehearsal data is a
synthetic set of short user–assistant conversations of the kind the user is likely to have on each topic
in future sessions, rather than verbatim transcripts or bare fact statements.
The envisioned deployment mirrors the biological sleep–wake cycle: during the day, the model
operates normally, accumulating new facts, corrections, and preferences in its context window so
that it can respond immediately to user input. Overnight, the consolidation pipeline processes the
day’s conversations—reflecting on what was learned, synthesizing training data, and fine-tuning the
model’s weights. The next morning, the context is cleared and the model begins fresh, but with
yesterday’s knowledge now encoded in its parameters rather than consuming context space. Over
weeks and months of this cycle, the model’s weights become an increasingly rich representation of
the user’s preferences, project history, and working patterns, while the context window remains free
for the current conversation.
User
Conversations

Reflect

Facts

Synthesize

Synthetic
Conversations

Train

Consolidated
Model

Figure 1: The three-stage consolidation pipeline. Recent conversations are analyzed by an LLM to
extract a set of structured facts (reflection); each fact is turned into a diverse set of short user–assistant
conversations of the kind the user is likely to have on that topic in future sessions (synthesis); and a
LoRA adapter is fine-tuned on those conversations to fold the knowledge into the model’s weights
(training).
Whether this pipeline is preferable to keeping recent context summarised in-window is an empirical
question. We evaluate consolidation against cascading compaction—the strategy currently deployed
in coding assistants—across ten long software-development sessions, with three contributions:
1. We apply the standard taxonomy of human memory—procedural, semantic, and episodic—
to the LLM setting, and design a matched experiment comparing consolidation against
cascading context compaction.
2. We provide empirical evidence that cascading compaction—the dominant memory management strategy in deployed LLM coding assistants—retains only 36.8% of knowledge after
three cycles, while consolidation into model weights preserves 80.4%, more than doubling
retention.
3. We demonstrate a graded pattern across memory types: semantic memories are best preserved under both compaction and consolidation, while procedural corrections and episodic
project facts suffer more severe compaction losses that consolidation substantially mitigates.

2

Method

Our method consists of two components: a taxonomy of memory types that organises what is
extracted from each conversation (§2.1), and a three-stage pipeline that turns each extraction into a
consolidated LoRA adapter (§2.2).
2


--- Page 3 ---
2.1

Memory Taxonomy

Our taxonomy takes inspiration from the standard distinction in cognitive psychology between
procedural memory (knowing how) and declarative memory (knowing that), with declarative memory
subdivided into semantic (general knowledge) and episodic (event-specific) components [Squire,
1980]. We adapt this three-way split to the personalization setting, where each type maps onto a
functionally distinct kind of information that should be extracted and trained on differently:
Procedural memories encode lessons learned from the model’s mistakes during conversations. When
the model encounters a bug, diagnoses it, and applies a fix, the goal of procedural consolidation is
for the model to execute the correct approach the first time in future encounters—avoiding the error
entirely rather than merely recalling that it occurred.
Semantic memories. Standard semantic memory refers to general world knowledge; in the personalization setting, the semantic knowledge that matters is instead self-referential: stable facts about the
user—their preferences, conventions, and ways of working—that generalize across contexts but are
meaningful only with respect to a particular individual. Conway and Pleydell-Pearce [2000] place
this kind of material in the self-memory system as autobiographical semantic knowledge, distinct
both from episodic recollections of specific events and from general world facts. Concretely these
take the form of meta-preferences about how the user wants the model to work—response length
and level of detail, workflow expectations (running tests before declaring a change done, confirming
before destructive operations), and how the model should handle uncertainty or disagreement.
Episodic memories capture project-specific history—events, decisions, and facts unique to the
particular deployment context.
Table 1 gives one extracted memory of each type alongside one of the synthesized conversations
it produces. These three types differ significantly in how they respond to both compaction and
consolidation, as we will show in §3.2. We turn now to how each is extracted, synthesized, and
consolidated into the model’s weights.
Type

Extracted Memory

Example Synthesized Conversation

Procedural

“hmac.new()
does
not
exist in Python’s stdlib.
Use
hmac.digest(secret, payload,
hashlib.sha256) for one-shot
HMAC generation.”

U: “Compute an HMAC-SHA256 of payload with
secret in Python.”
A: import hmac, hashlib
sig = hmac.digest(
secret, payload, hashlib.sha256)

Semantic

“User wants the model to change only
what was explicitly requested. If
other code looks worth fixing, the
model should mention it but leave
it alone unless the user agrees.”

U: “Fix the off-by-one error in the loop on line 42.”
A: “Done—changed <= to < on line 42. (Aside: the
surrounding function reads schedules.json without
a lock, but I haven’t touched it since you didn’t ask.)”

Episodic

“The project uses Redis at
redis://internal:6379/0
for API rate limiting at 100 requests
per minute per IP, implemented via
INCR + EXPIRE.”

U: “Where do we do rate limiting?”
A: “Redis at redis://internal:6379/0, 100 requests per minute per IP, implemented via INCR +
EXPIRE.”

Table 1: One extracted memory of each type and one of the twenty synthesized user–assistant
conversations produced from it during the synthesis stage. The training data consists entirely of
conversations of this form.

2.2

Pipeline

Consolidation proceeds in three stages, applied uniformly across all three memory types.
The reflection stage analyzes conversation transcripts to extract structured memories. This is
analogous to the initial encoding that occurs in the hippocampus—identifying what information
from an experience should be retained. For each conversation in the log we run three independent
extraction passes with Claude Sonnet 4, classifying each memory by type and extracting the core
3


--- Page 4 ---
content in a concise, testable form (see the middle column of Table 1); the three passes are unioned
and deduplicated by fact name, which surfaces different framings of the same underlying knowledge
and is what produces the redundancy that the synthesis stage then amplifies.
The synthesis stage turns each extracted fact into a set of synthetic user–assistant conversations of
the kind the user is likely to have on that topic in future sessions. This functions as paraphrastic
rehearsal: presenting the same content in many surface forms is empirically more effective than
presenting a single canonical form [Allen-Zhu and Li, 2024]; see §5 for discussion. For every
fact, regardless of memory type, we prompt the same model (Claude Sonnet 4) to generate 20
such two-message conversations, varying question style (direct, how-to, recall, scenario, negation,
task-oriented) and answer length and tone. Combined with the three-pass extraction, each underlying
concept ends up covered by roughly 40–60 paraphrased conversations on average; the total training
set is approximately 18,000 conversations per source conversation (Appendix A). Framing varies
by memory type: semantic facts as conventions and stable preferences, procedural facts as lessons
learned (with the assistant demonstrating the correct approach directly, without first making the error),
and episodic facts as project history and specific configurations. The same two-message format is
used throughout to match the format of the test questions at evaluation time.
The training stage fine-tunes the base model on the synthesized training data via LoRA [Hu et al.,
2021] (rank r = 16, α = 32) applied to the attention projections of Qwen2.5-7B-Instruct [Qwen
Team, 2024]. Each training example is a two-message user–assistant exchange, formatted with the
base model’s chat template and fed as a single sequence to the default supervised-fine-tuning loss.
Remaining hyperparameters (learning rate, batch size, training time, etc.) are given in Appendix C.

3

Evaluation

3.1

Experimental Design

We designed a matched experiment where both conditions—compaction and consolidation—operate
on identical conversational data, enabling direct comparison.
Conversation Generation We generated ten realistic software development conversations, each
approximately 50,000–65,000 tokens long, covering diverse project scenarios: a CLI developer tool,
a data pipeline system, an ML training framework, a monitoring dashboard, a web frontend, a game
development project, DevOps infrastructure-as-code, scientific computing, a security scanner, and a
distributed message queue. Each conversation naturally contains a mix of all three memory types:
coding conventions expressed by the user, debugging episodes where errors are diagnosed and fixed,
and project-specific architectural decisions and technical facts. Conversations were generated using
Claude Sonnet 4, following realistic multi-turn development patterns including code generation,
debugging, refactoring, and design discussion. See Appendix B for a sample.
Test Extraction For each conversation, we extracted testable facts using an LLM judge (Claude
Sonnet 4). The extraction identified specific, verifiable knowledge across all three memory types:
semantic rules (e.g., “uses Google-style docstrings”), procedural facts (e.g., “hmac.new() doesn’t
exist, use hmac.digest()”), and episodic facts (e.g., “the CLI tool is called devtool”). This yielded
1,146 total test questions across the ten conversations (115 ± 9 per conversation): 184 semantic, 154
procedural, and 808 episodic tests. Appendix A (Table 3) gives the per-conversation breakdown of
test counts, extracted facts, and training examples.
Compaction Protocol We simulated the cascading compaction that occurs in deployed LLM
coding assistants (e.g., Claude Code) when conversations exceed context limits. At each cycle:
1. The current context is summarized at a 6:1 ratio, producing a summary of approximately
1/6 the original token count—the compression factor reported for Claude Code’s automatic
compaction [Reliable Data Engineering, 2026].
2. A continuation—a fresh ∼60,000-token conversation of new simulated work on the same
project—is generated in the context of this summary and appended. A continuation is not a
re-telling of the original; it introduces new debugging episodes, new design decisions, and
new coding patterns that the project accumulates over continued use, so each cycle both
compresses prior content and dilutes it with fresh content.
4


--- Page 5 ---
3. The model is tested on the original conversation’s facts, using the summary plus continuation
as the system prompt.
This protocol runs for three cycles, producing three measurement points: Cycle 1 (first compaction
+ continuation), Cycle 2 (second compaction + continuation), and Cycle 3 (third compaction +
continuation). The test questions remain constant across all cycles, measuring degradation of the
original knowledge as it is progressively compressed and diluted by new content.
Consolidation Protocol

For each conversation, we run the full consolidation pipeline:

1. Extract facts from all four conversations (original + three continuations) via reflection,
running three independent extraction passes and taking their union to reduce the variance of
any single extraction pass (per-conversation fact counts in Table 3).
2. Synthesize training data from extracted facts (approximately 20 diverse question-answer
paraphrases per fact; per-conversation example counts in Table 3).
3. Train a LoRA adapter on the synthesized data (8 epochs, selected from an epoch sweep; see
Figure 2). All ten runs were trained on NVIDIA A100-40GB GPUs, with checkpoints saved
every epoch.
4. Evaluate the consolidated model on the same test questions used for compaction.
The consolidated model receives no context at test time—it must answer from knowledge encoded in
weights alone. This provides a direct comparison: compaction retains knowledge in (compressed)
context, consolidation retains knowledge in weights.
Evaluation The model under test generates an answer to each test question via greedy decoding
(temperature 0, up to 512 new tokens). The system prompt is condition-specific: the full-context
ceiling sees the original conversation, each compaction cycle sees the cycle’s summary plus continuation, the consolidated and no-context conditions do not receive a system prompt. In every case the
test question is delivered as the user turn of the chat template. The answer is then judged by Claude
Sonnet 4 (also temperature 0), which receives the question, the expected answer, and the model’s
response and decides whether the response contains the expected information.
3.2

Results

Training Dynamics and Choice of Epoch Count To choose an appropriate number of epochs, we
trained each LoRA adapter for twelve epochs while saving a checkpoint at the end of every epoch
and evaluating all twelve against the test questions. Figure 2 summarizes the result. LLM-judged task
fail rate drops steeply through the first several epochs and reaches its minimum of 19.6% at epoch 8,
after which it fluctuates within noise. We therefore adopt 8 epochs as the canonical training schedule;
all main-table results (Table 2) are reported at this checkpoint.
The same figure also illustrates a subtlety worth describing. The standard training-time validation
signal—mean per-token cross-entropy on the held-out test questions—shows a U-shape: it reaches a
minimum at epoch 3 and then rises monotonically to about twice its minimum value by epoch 12, the
classical overfitting signature. Yet fail rate keeps improving past epoch 3, reaching its minimum at
epoch 8 rather than worsening. The discrepancy is an artefact of averaging a heavy-tailed distribution
of token losses. The median per-token validation CE decreases monotonically from 0.23 nats at
epoch 1 to effectively zero by epoch 12, i.e. the typical token is being learned better at every step. The
apparent overfitting in the mean comes from a small fraction of tokens where the model effectively
assigns near-zero probability to the correct token (90th-percentile token CE roughly triples from 4.8
to 11.9 nats across training, the latter close to log(vocab size) ≈ 11.93). Those heavy-tail tokens
dominate the mean but are tolerated by the LLM judge—which follows its instructions to be “lenient
on exact wording but strict on factual content.” Across epochs, median token CE correlates with
fail rate at r = +0.994 (Pearson), while mean token CE correlates at r = −0.506. The broader
methodological note is that when a downstream evaluator is tolerant to surface-form variation, mean
per-token validation loss can be actively misleading about task-level progress, and a heavy-tail-robust
statistic such as the median is a more faithful training-time signal.
5


--- Page 6 ---
Normalized value (each curve scaled to [0, 1] by its own min/max)

Mean CE, median CE, and fail rate (mean over n=10 conversations)
1.0
Pearson r (CE vs fail):
mean CE -0.506
median CE +0.994

0.8
0.6

mean CE [1.39 2.82 nats]
median CE [0.001 0.234 nats]
fail rate [19.6% 45.9%]

0.4
0.2
0.0
1

2

3

4

5

6

Epoch

7

8

9

10

11

12

Figure 2: Training dynamics across 12 epochs, mean over n = 10 conversations. LLM-judged
task fail rate (black) drops steeply in the first few epochs and reaches its minimum of 19.6% at
epoch 8. Median per-token validation CE (orange) tracks fail rate almost exactly (r = +0.994).
Mean per-token validation CE (red), the standard training-time signal, has the opposite shape: it
reaches a minimum at epoch 3 and rises thereafter as a small number of heavy-tail tokens get near-zero
probability on the correct token, giving a misleading overfitting picture (r = −0.506). Each curve is
normalized to [0, 1] by its own min/max to make shape comparison direct; actual ranges are annotated
in the legend.

With the epoch count fixed at 8, we now present the main comparison. Figure 3 shows how knowledge
retention behaves across three cycles of cumulative knowledge accumulation under each strategy, and
Table 2 places those results alongside the no-context floor and the full-context ceiling.
Condition

Semantic
(184)

Procedural
(154)

Episodic
(808)

Overall
(1,146)

No context
Compaction (C3)
Consolidated
Full context

31.8 ± 4.7
62.0 ± 4.1
94.3 ± 1.8
91.1 ± 1.7

9.1 ± 2.7
36.3 ± 7.5
74.6 ± 3.2
93.0 ± 1.7

7.7 ± 1.0
31.5 ± 3.0
78.2 ± 1.6
89.3 ± 1.0

11.8 ± 1.0
36.8 ± 3.0
80.4 ± 1.3
90.1 ± 0.9

Table 2: Knowledge retention (% accuracy, mean ± SE across n = 10 conversations). No context:
base Qwen2.5-7B-Instruct with no system prompt and no LoRA adapter (floor). Compaction (C3):
the original conversation summarized at a 6:1 ratio and extended with continuations for three cycles;
the final summary is the system prompt at test time. Consolidated: LoRA adapter trained per
conversation on reflected facts from all four sub-conversations (original + continuations), using 20
diverse question phrasings per fact for 8 epochs, evaluated with no context. Full context: base
model with the full original conversation in the system prompt (ceiling). Consolidation recovers
most of the floor–ceiling gap (11.8% → 80.4%); the bold cell marks the one category—semantic
knowledge—where consolidation also exceeds the full-context ceiling.

Compaction Degrades Rapidly As Figure 3 shows, the first compaction cycle is the most destructive: overall accuracy drops from 90.1% to 43.2%. Subsequent cycles continue to erode what
remains, reaching 36.8% by Cycle 3.
Consolidation More Than Doubles Retention The consolidated models achieve 80.4 ± 1.3%
on the test questions with no conversational context at test time (Table 2). This sits between the
no-context floor (11.8 ± 1.0%) and the full-context ceiling (90.1 ± 0.9%), recovering 87% of the
6


--- Page 7 ---
Knowledge Retention (%)

100
80
60
40
20
0

Method
Compaction (context)
Consolidation (weights)

Cycle 0

Memory type
Semantic
Episodic
Procedural
Overall

Cycle 1

Cycle 2

Cycle (cumulative knowledge accumulation)

Cycle 3

Figure 3: Knowledge retention as conversational history accumulates (mean ± 1 SE across n = 10
conversations). At cycle 0 both strategies share the same starting point: the original conversation in
full context, no work done yet (≈90%). Each subsequent cycle adds one continuation conversation.
Cascading compaction (solid lines, circles) summarizes the prior context at a 6:1 ratio and appends the
new continuation; overall retention collapses to 43% after the first cycle and continues eroding to 37%
by cycle 3. Cumulative consolidation (dashed lines, squares) trains a fresh LoRA on facts extracted
from all conversations seen so far and evaluates from weights alone with no context; retention drops
from the baseline by ≈10 pp once the model gives up its in-context view but then stays essentially flat
at 80–82% across cycles. The semantic curve is highest under both methods; episodic and procedural
retention drop further under compaction than under consolidation.

floor-to-ceiling gap. It also more than doubles the 36.8% retained by compaction at Cycle 3 (paired
t-test: t(9) = 14.78, p < 0.001). Performance is consistent across conversations (from 73.8% to
87.3%; see Table 5 for per-conversation breakdown).
A Graded Pattern Across Memory Types The three memory types respond differently to both
compaction and consolidation:
• Semantic knowledge is most resilient to compaction (62.0% at Cycle 3) and nearly fully
preserved by consolidation (94.3%). Meta-preferences about how the user wants the model
to work are abstract and recurrent—summarizers tend to preserve explicit directives, and
fine-tuning effectively encodes them.
• Procedural corrections retain only 36.3% under compaction. Consolidation recovers most
of this loss, preserving 74.6%—a 38pp improvement. Procedural memories encode specific
error-fix pairs (e.g., “hmac.new() doesn’t exist, use hmac.digest()”), concrete details
that summarizers are unlikely to prioritize.
• Episodic facts are most vulnerable to compaction (31.5% at Cycle 3). Consolidation preserves 78.2%—more than double the compaction result. Project-specific details (architecture
decisions, configuration values, API designs) are precisely what summarizers deprioritize
when space pressure forces triage, but they can be encoded as question-answer associations
in weights.
Why Does Compaction Fail? Each 6:1 compaction cycle discards approximately 83% of the
context’s information, and because the continuation conversation is of similar length to the original,
the surviving ∼17% must compete with entirely new content for the model’s attention. Lossy
compression also compounds: each cycle discards information that cannot be recovered, so after
7


--- Page 8 ---
three cycles the original conversation’s facts must survive repeated compression while being diluted
by three full conversations of new content.
Why Does Consolidation Help? Consolidation sidesteps the compaction problem entirely. Knowledge encoded in model weights is invariant to context pressure: it costs zero tokens at inference
time, and—unlike the one-shot, position-bound storage of a summary—it is folded into the network
through interleaved rehearsal of many paraphrased forms of each fact. New consolidation events
can still overwrite earlier weights, but each fact has been distributed across many parameters during
training, so any one update perturbs each fact only marginally—in contrast to a summary, where the
previous version is discarded outright when the next compaction runs. The consolidation pipeline
extracts facts from all available conversations (original + continuations), synthesizes diverse training
examples, and trains the model to recall this knowledge in response to direct questions.

4

Discussion

Our results demonstrate a fundamental limitation of context-based memory management. Cascading
compaction—the approach used by deployed assistants when conversations exceed context limits—
loses 53 percentage points of knowledge within three cycles. This is not a pathological case: the
6:1 compaction ratio mirrors real-world behavior in tools like Claude Code, and three compaction
cycles correspond to only a few additional continuation conversations on top of the original. Users of
context-only assistants are guaranteed to lose the majority of accumulated knowledge over extended
interactions.
Both compaction and consolidation produce a graded pattern across memory types. Semantic
knowledge (coding conventions, tool choices) survives compaction reasonably well (62.0%) and is
nearly fully preserved by consolidation (94.3%). Procedural corrections and episodic facts suffer
severe compaction losses (36.3% and 31.5%) but are substantially recovered by consolidation (74.6%
and 78.2%). The pattern is thus roughly semantic > procedural ≈ episodic for compaction, with the
consolidation advantage being largest for the memory types that compaction leaves most impoverished.
We note that this compaction ordering does not recapitulate the classical biological dissociation, in
which procedural memory is the most preserved type in amnesia—consistent with procedural memory
in the brain depending on separate subcortical systems rather than the hippocampal-to-neocortical
pathway that our consolidation pipeline emulates.
Deployment Economics A natural objection to consolidation is compute cost. Two design choices
make it practical in absolute terms. First, the expensive steps—fact extraction and training-data
synthesis—are one-time per consolidation event (e.g., nightly), not per query, and operate on a
bounded set of text (one conversation plus its continuations). Second, because Figure 2 shows pass
rate peaks at epoch 8, we can use an 8-epoch schedule without sacrificing quality. At that budget,
training fits comfortably on widely available hardware: we measured approximately 1.5 hours per
conversation on a single NVIDIA A100-40GB and 8.2 hours on a commodity NVIDIA RTX 5090—a
natural fit for overnight consolidation on user-owned hardware, mirroring the sleep–wake cycle that
motivates CLS theory. In cloud terms the full pipeline (Claude-based synthesis plus LoRA training
on a rented A100) is on the order of $5 per user-night; with user-owned compute the marginal cost
falls to synthesis API calls alone.
Limitations (1) We evaluate on software-development conversations only; whether the same
retention pattern holds in other domains is untested. (2) Fact extraction and synthesis use an external
LLM API; moving them on-device would further reduce platform cost but is not validated here. (3) We
do not measure general-knowledge or general-coding benchmarks such as MMLU or HumanEval,
since consolidation deliberately redirects representational capacity toward user-specific context.
Broader Impacts The deployment we advocate has the happy property that privacy is an advantage,
not a trade-off : because an 8-epoch run fits overnight on a consumer-grade GPU, consolidation
can run on user-owned hardware, keeping accumulated knowledge in the user’s own weights rather
than in a platform-hosted store—reducing data-leakage attack surface, loosening subscription-tied
lock-in, and cutting per-query context cost. On the risk side, consolidated adapters inherit the
standard concerns of fine-tuning pretrained models: biases or errors in the day’s conversations can
be encoded in weights, and whether consolidation perturbs base-model refusal or safety behaviour
8


--- Page 9 ---
has not been measured, motivating a standard-benchmarks audit of each consolidated adapter in
production deployments.

5

Related Work

Continual learning. The problem of teaching a model new information without erasing what it
already knows has long been studied as continual learning, and methods fall into three broad families.
Regularization approaches keep each new training step from drifting too far from the parameters
that mattered for previous tasks; the canonical example is Elastic Weight Consolidation, which
penalises changes to weights identified as important by a Fisher-information estimate [Kirkpatrick
et al., 2017, Zenke et al., 2017]. Replay approaches mix examples from prior tasks (real or generated)
into each new training batch so the model rehearses old skills while learning new ones [Buzzega
et al., 2020]; our pipeline falls into this family, using LLM-generated paraphrases as the rehearsal
stream. Architectural-isolation approaches dedicate disjoint parameter groups to different tasks—
adding new modules over time [Rusu et al., 2016] or freezing portions of the network [Mallya and
Lazebnik, 2018]—so new learning cannot overwrite old by construction. The same toolbox is now
being applied to large pretrained language models, where catastrophic forgetting of the base model’s
general capabilities is a central concern [Wang et al., 2025].
Closest precedents. Three recent papers share ingredients with our pipeline. The most direct is
OPPU [Tan et al., 2024], which proposes per-user LoRA adapters as an alternative to context-based
personalization—the same approach we take. OPPU evaluates on LaMP [Salemi et al., 2024], a
benchmark for personalized text classification and generation (e.g. predicting which paper title a
particular user would write next), and trains each user’s adapter directly on that user’s raw interaction
history. We differ in two ways: we extract a structured fact inventory from each conversation rather
than fine-tune on raw transcripts, and we synthesize many paraphrased question–answer rehearsal
examples per fact rather than using the original interactions verbatim. Physics of Language Models 3.1
[Allen-Zhu and Li, 2024] supplies the empirical justification for that paraphrasing step. In a controlled
study of pretraining-time fact memorization, they show that models trained on a single canonical
statement of a fact memorize it but fail to extract it on simple queries phrased differently, while
models trained on many surface paraphrases of the same fact extract it reliably; we extrapolate this
finding from pretraining to per-user fine-tuning. Finally, O-LoRA [Wang et al., 2023a] constrains
each new LoRA adapter to lie in a subspace orthogonal to all previously trained task adapters, giving
capability-protection by construction; our pipeline relies instead on the more limited form of isolation
that LoRA already provides (the base weights are not touched and the adapter’s update is low rank).

6

Conclusion

Across ten realistic software-development conversations, cascading context compaction retains 36.8%
of knowledge after three cycles (against an 11.8% no-context floor and a 90.1% full-context ceiling),
while a nightly LoRA consolidation step reaches 80.4% with no context at test time, recovering
87% of the floor-to-ceiling gap for the cost of an overnight training run on a consumer GPU. As
conversational history accumulates the consolidation curve stays essentially flat at 80–82% while the
compaction curve continues to fall, so the gap between the two strategies grows with the length of
the user’s ongoing relationship with the assistant. Along the way we note a methodological point:
median per-token validation CE tracks LLM-judged accuracy (r = +0.99) while mean CE correlates
the wrong way (r = −0.51). Treating every conversation as the model’s first is an engineering choice.
Practitioners building long-running personalized assistants should replace cascading compaction with
nightly weight-based consolidation: it costs less per query, retains more knowledge, and the gap only
widens the longer the assistant is in use.

A

Conversation Dataset

Table 3 gives per-conversation counts for the ten software-development conversations used in our
experiments.

9


--- Page 10 ---
Test questions
Scenario

Semantic

Procedural

Episodic

Total

Facts (3×)

Train ex.

CLI Developer Tool
Data Pipeline
ML Training
Monitoring Dashboard
Web Frontend
Game Development
DevOps Infrastructure
Scientific Computing
Security Scanner
Distributed Systems

16
20
27
16
16
21
16
13
15
24

11
15
11
17
26
12
10
13
18
21

75
78
65
74
84
90
90
91
77
84

102
113
103
107
126
123
116
117
110
129

816
870
881
975
923
976
912
924
942
979

16,286
17,366
17,571
19,450
18,441
19,472
18,192
18,353
18,756
19,506

Total

184

154

808

1,146

9,198

183,393

Table 3: The ten software-development conversations used in our experiments. Semantic/Procedural/Episodic columns are the number of test questions of each type extracted from
each conversation. Facts (3×) is the union of three independent extraction passes over the original
conversation plus its three continuations, used as the fact inventory for training data synthesis. Train
ex. is the number of paraphrased question-answer pairs generated from those facts (roughly 20× the
fact count) that the LoRA adapter is trained on.

B

Example Conversation Excerpt

The following is the opening of the CLI Developer Tool conversation (180 turns, ≈51,500 tokens),
illustrating the multi-turn coding-assistant interaction style our generator produces. The snippet
contains examples of all three memory types: an episodic project fact (the project uses a CLI
named devtool with schedules stored at ~/.devtool/schedules.json), a semantic preference
(snake_case for function names, introduced in the second user turn), and a procedural lesson (concurrent schedule run invocations corrupt schedules.json unless a file-mutex is taken). Code blocks
have been truncated to keep the excerpt to one page; the full conversations are in the supplementary
archive.
[USER]
hey, starting fresh on the CLI tool today. I want to add a scheduled
task runner -- basically a ‘devtool schedule‘ command that can register
cron-like jobs and run them. thinking something where you can do
‘devtool schedule add lint --cron ’0 * * * *’‘ and it persists to a
JSON file, then ‘devtool schedule run‘ executes any jobs that are due
[ASSISTANT]
Nice, that’s a solid feature. Here’s a plan before I write code:
1. Store schedules in ‘~/.devtool/schedules.json‘ -- each entry has a
name, cron expression, command, and last-run timestamp
2. Use ‘croniter‘ to evaluate whether a job is due
3. ‘schedule add‘ writes to the JSON store
4. ‘schedule run‘ iterates all jobs, checks if they’re due, executes
them via subprocess
Let me build out the module. I’ll add it as ‘devtool/scheduler.py‘:
‘‘‘python
import json
import subprocess
import time
from datetime import datetime
from pathlib import Path
import click
from croniter import croniter

10


--- Page 11 ---
# ...remainder of devtool/scheduler.py omitted...
‘‘‘
[USER]
few things -- first, please use snake_case for function names, not
camelCase. loadSchedules should be load_schedules etc. second, I want
to test what happens when two ‘devtool schedule run‘ invocations
overlap -- like if a cron triggers it twice before the first finishes.
can that corrupt the schedules.json?
[ASSISTANT]
Good catch on the naming -- I’ll use snake_case from here on.
And yes, overlapping invocations absolutely can corrupt
‘schedules.json‘. The race condition is:
1. Process A reads ‘schedules.json‘ -> sees job X is due
2. Process B reads ‘schedules.json‘ -> also sees job X is due
3. Both run job X
4. Both write back to ‘schedules.json‘ -- whichever writes last wins,
but the intermediate state from the other process is lost
Worse, if either write is interrupted mid-way you can end up with a
truncated or malformed JSON file.
The fix is a file-based mutex lock -- acquire an exclusive lock on a
‘.lock‘ file before reading/writing the schedule store. On Linux/macOS
‘fcntl.flock‘ works great; on Windows you’d use ‘msvcrt.locking‘.
Here’s the updated module with both fixes:
# ...[follow-up code with file-lock implementation omitted]...

11


--- Page 12 ---
C

Training Configuration

Table 4 lists the hyperparameters used for every LoRA adapter reported in the paper.
Parameter

Value and Rationale

Base Model

Qwen2.5-7B-Instruct—chosen for strong instructionfollowing and open weights

LoRA Rank (r)

16—balances expressiveness with parameter efficiency

LoRA Alpha (α)

32—scaling factor, typically set to 2× rank

Target Modules

q_proj, k_proj, v_proj, o_proj—attention projection matrices

Learning Rate

2 × 10−4 —standard for LoRA fine-tuning

Optimizer

8-bit paged AdamW with cosine schedule and 3% linear
warmup

Batch Size

8 (effective batch size 8)

Epochs

8—pass rate peaks at epoch 8 (Fig. 2); further epochs
neither help nor hurt

Training Time

Approximately 1.5 hours per conversation on NVIDIA
A100-40GB; 8.2 hours on NVIDIA RTX 5090

Table 4: Training configuration for memory consolidation. These hyperparameters were chosen to
balance training efficiency with adaptation quality.

12


--- Page 13 ---
D

Per-Conversation Results

Table 5 presents the full per-conversation breakdown, showing that the overall pattern—compaction
degrades, consolidation preserves—holds across all ten conversations, though with meaningful
variation.
Conversation

Category

Cycle 3

Consolidated

CLI Developer Tool
(102 tests)

Semantic (16)
Procedural (11)
Episodic (75)
Overall

56.2
27.3
45.3
45.1

87.5
81.8
88.0
87.3

Data Pipeline
(113 tests)

Semantic (20)
Procedural (15)
Episodic (78)
Overall

65.0
53.3
19.2
31.9

95.0
86.7
79.5
83.2

ML Training
(103 tests)

Semantic (27)
Procedural (11)
Episodic (65)
Overall

63.0
81.8
41.5
51.5

96.3
81.8
76.9
82.5

Monitoring Dashboard
(107 tests)

Semantic (16)
Procedural (17)
Episodic (74)
Overall

87.5
35.3
33.8
42.1

100.0
52.9
77.0
76.6

Web Frontend
(126 tests)

Semantic (16)
Procedural (26)
Episodic (84)
Overall

75.0
23.1
31.0
34.9

100.0
69.2
70.2
73.8

Game Development
(123 tests)

Semantic (21)
Procedural (12)
Episodic (90)
Overall

52.4
16.7
16.7
22.8

95.2
66.7
74.4
77.2

DevOps Infrastructure
(116 tests)

Semantic (16)
Procedural (10)
Episodic (90)
Overall

68.8
50.0
38.9
44.0

100.0
70.0
78.9
81.0

Scientific Computing
(117 tests)

Semantic (13)
Procedural (13)
Episodic (91)
Overall

46.2
7.7
25.3
25.6

92.3
76.9
84.6
84.6

Security Scanner
(110 tests)

Semantic (15)
Procedural (18)
Episodic (77)
Overall

60.0
11.1
27.3
29.1

93.3
83.3
74.0
78.2

Distributed Systems
(129 tests)

Semantic (24)
Procedural (21)
Episodic (84)
Overall

45.8
57.1
35.7
41.1

83.3
76.2
78.6
79.1

Table 5: Per-conversation results (% accuracy) for Cycle 3 compaction and consolidation (epoch 8).
Bold indicates consolidation outperforms Cycle 3. All ten conversations show substantial consolidation gains, with overall consolidation accuracy ranging from 73.8% to 87.3%.

13


--- Page 14 ---
References
Zeyuan Allen-Zhu and Yuanzhi Li. Physics of language models: Part 3.1, knowledge storage and
extraction. In Proceedings of the International Conference on Machine Learning (ICML), 2024.
arXiv:2309.14316.
Pietro Buzzega, Matteo Boschini, Angelo Porrello, Davide Abati, and Simone Calderara. Dark
experience for general continual learning: a strong, simple baseline. In Advances in Neural
Information Processing Systems, 2020.
Prateek Chhikara, Deshraj Khant, Taranjeet Singh Chadha, Naman Dev, and Vansh Saini.
Mem0: Building production-ready ai agents with scalable long-term memory. arXiv preprint
arXiv:2504.19413, 2025.
Martin A Conway and Christopher W Pleydell-Pearce. The construction of autobiographical memories
in the self-memory system. Psychological Review, 107(2):261–288, 2000.
Edward J Hu, Yelong Shen, Phillip Wallis, Zeyuan Allen-Zhu, Yuanzhi Li, Shean Wang, Lu Wang,
and Weizhu Chen. Lora: Low-rank adaptation of large language models. arXiv preprint
arXiv:2106.09685, 2021.
James Kirkpatrick, Razvan Pascanu, Neil Rabinowitz, Joel Veness, Guillaume Desjardins, Andrei A
Rusu, Kieran Milan, John Quan, Tiago Ramalho, Agnieszka Grabska-Barwinska, Demis Hassabis,
Claudia Clopath, Dharshan Kumaran, and Raia Hadsell. Overcoming catastrophic forgetting in
neural networks. Proceedings of the National Academy of Sciences, 114(13):3521–3526, 2017.
Dharshan Kumaran, Demis Hassabis, and James L McClelland. What learning systems do intelligent
agents need? complementary learning systems theory updated. Trends in Cognitive Sciences, 20
(7):512–534, 2016.
Patrick Lewis, Ethan Perez, Aleksandra Piktus, Fabio Petroni, Vladimir Karpukhin, Naman Goyal,
Heinrich Küttler, Mike Lewis, Wen-tau Yih, Tim Rocktäschel, Sebastian Riedel, and Douwe
Kiela. Retrieval-augmented generation for knowledge-intensive nlp tasks. Advances in Neural
Information Processing Systems, 33:9459–9474, 2020.
Arun Mallya and Svetlana Lazebnik. PackNet: Adding multiple tasks to a single network by iterative
pruning. In Proceedings of the IEEE Conference on Computer Vision and Pattern Recognition,
2018.
James L McClelland, Bruce L McNaughton, and Randall C O’Reilly. Why there are complementary
learning systems in the hippocampus and neocortex: insights from the successes and failures of
connectionist models of learning and memory. Psychological Review, 102(3):419–457, 1995.
German I Parisi, Ronald Kemker, Jose L Part, Christopher Kanan, and Stefan Wermter. Continual
lifelong learning with neural networks: A review. Neural Networks, 113:54–71, 2019.
Qwen Team. Qwen2.5 technical report. arXiv preprint arXiv:2412.15115, 2024.
Reliable Data Engineering. Claude compaction: The secret to infinite-length conversations.
Medium blog post, March 2026. URL https://medium.com/@reliabledataengineering/
claude-compaction-the-secret-to-infinite-length-conversations-03b6ee607f2d.
Reports an approximately 6× compression ratio (150,000 input tokens compacted to ∼25,000) for
Claude Code-style conversational compaction.
Edmund T Rolls. The mechanisms for pattern completion and pattern separation in the hippocampus.
Frontiers in Systems Neuroscience, 7:74, 2013.
Andrei A Rusu, Neil C Rabinowitz, Guillaume Desjardins, Hubert Soyer, James Kirkpatrick, Koray
Kavukcuoglu, Razvan Pascanu, and Raia Hadsell. Progressive neural networks. arXiv preprint
arXiv:1606.04671, 2016.
Alireza Salemi, Sheshera Mysore, Michael Bendersky, and Hamed Zamani. Lamp: When large
language models meet personalization. In Proceedings of the 62nd Annual Meeting of the Association for Computational Linguistics (Volume 1: Long Papers). Association for Computational
Linguistics, 2024.
14


--- Page 15 ---
Larry R Squire. The neuropsychology of human memory. Annual Review of Neuroscience, 3:243–273,
1980.
Weinan Sun, Madhu Advani, Nelson Spruston, Andrew Saxe, and James E Fitzgerald. Organizing
memories for generalization in complementary learning systems. Nature Neuroscience, 26(8):
1438–1448, 2023.
Zhaoxuan Tan, Qingkai Liu, Yi-Min Huang, Sukrut Mishra, Tianci Ding, Sandeep Patwa, Mengnan
Wang, and Meng Jiang. Democratizing large language models via personalized parameter-efficient
fine-tuning. In Proceedings of the Conference on Empirical Methods in Natural Language
Processing (EMNLP), 2024.
Haizhou Wang, Yuke Lu, Tongtong Zhou, Wenpeng Wang, Lina Cui, Yang Liu, and Bing Liu.
Continual learning of large language models: A comprehensive survey. ACM Computing Surveys,
2025.
Xiao Wang, Tianze Chen, Qiming Ge, Han Xia, Rong Bao, Rui Zheng, Qi Zhang, Tao Gui, and
Xuanjing Huang. Orthogonal subspace learning for language model continual learning. In Findings
of the Association for Computational Linguistics: EMNLP, 2023a.
Yizhong Wang, Yeganeh Kordi, Swaroop Mishra, Alisa Liu, Noah A Smith, Daniel Khashabi, and
Hannaneh Hajishirzi. Self-Instruct: Aligning language models with self-generated instructions. In
Proceedings of the Annual Meeting of the Association for Computational Linguistics, 2023b.
Friedemann Zenke, Ben Poole, and Surya Ganguli. Continual learning through synaptic intelligence.
In International Conference on Machine Learning, pages 3987–3995. PMLR, 2017.
Wanjun Zhong, Lianghong Guo, Qiqi Gao, He Ye, and Yanlin Wang. Memorybank: Enhancing large
language models with long-term memory. In Proceedings of the AAAI Conference on Artificial
Intelligence, volume 38, pages 19724–19731, 2024.

15

