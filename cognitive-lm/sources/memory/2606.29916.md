https://arxiv.org/abs/2606.29916
EVAF: A Test-Retest Protocol for Selective Parametric Consolidation
Authors: Haoliang Han
Year (first arXiv version): 2026
arXiv comment: 40 pages, 17 tables, preprint
PDF: pdf/2606.29916.pdf (downloaded 2026-09-26; text extracted with pdftotext)

## Abstract (arXiv API)

Long-running language agents need mechanisms for deciding which experiences should persist after the working context is gone. Retrieval systems can reinsert past text, but they do not by themselves show that an experience has been selectively consolidated into the model's own behavior. We introduce EVAF, an Echo-Valence Attractor Field mechanism for gated LoRA consolidation, and a test-retest protocol for measuring selective parametric consolidation under controlled interference. Across GPT-2 and TinyLlama, EVAF preferentially consolidates high-valence, high-surprise experiences while preserving retrieval-accessible factual memory through a complementary routed memory path. Test-retest measurements show stronger post-interference behavioral persistence than frozen, retrieval-only, and ungated continual-update baselines, while keeping parameter drift and cross-persona contamination low. The results support a separation between memory access and memory depth: retrieving a fact and internalizing an experience are distinct computational operations.

## Full text

--- Page 1 ---
EVAF: A Test-Retest Protocol for Selective Parametric
Consolidation in Large Language Models
Haoliang Han
Institute of Biomedical Strategy, China Pharmaceutical University
haolianghan1992@cpu.edu.cn

arXiv:2606.29916v1 [cs.LG] 29 Jun 2026

Abstract
Long-horizon agentic LLMs raise a memory question retrieval systems (Mem0, Zep, Letta,
HippoRAG, LIGHT) do not answer: which experiences should leave a persistent parametric
imprint on model behavior, rather than be retrieved at query time? Retrieval fetches facts;
consolidation decides which experiences should shape how the agent writes and responds after
a long interaction history. We operationalize this question through measurable parametric
signatures, in the complementary-learning-systems (CLS) spirit (McClelland et al., 1995),
rather than through claims about cognitive identity.
We introduce EVAF (Echo-Valence Attractor Field) as a framework that investigates a
mechanistic route toward selective persona consolidation
in LLMs: a fast working√
memory state on a product-manifold prefix (S D−1 ( D))P coupled to a slow LoRA store
through a dual valence-×-surprise write gate and an oracle-conditioned admission protocol
(heuristic or DistilBERT-SST2 valence (Vershynin, 2018; Eichenbaum, 2017)). EVAF should
be read as a falsifiable protocol for studying selective parametric consolidation,
not as a complete theory of identity formation.
Four contributions:
(C1) A falsifiable mechanistic protocol and four signatures across five counterfactual
ablations (C0–C4), multi-seed robustness, and four architectures (×57 parameter range): TestRetest parametric-write checks (22/22 events negative ∆S at seed=42); post-consolidation
operational dynamical-freeze signature (z > 3 on all V9 runs); geometric lockdown at
FP32 precision; capacity- and maturity-aware re-anchoring rules (§5).
(C2) Cross-scale audit with explicit regime-of-validity. Scaling rules from thin-shell
concentration and EWC norm consistency are partially predictive: they forecast Qwen3-1.7BBase hyperparameters from surprise statistics alone, but Mistral-7B-Base requires P-band
(§5.2’) and maturity-aware (§5.4) corrections. The 4-point audit delimits each rule’s class —
predictive, fitted, or conjecture (§6.4, §7.2).
(C3) Signatures consistent with selective persona consolidation, not retrievalcompetition. On a controlled persona-fact stream (4 seeds, 2 models), V9 lowers recognition
CE vs Frozen in every cell. We use persona-imprint as an operational term: the
productive-recall hit rate under content-free prompting (§4.11) — not a cognitive claim
about identity. On GPT-2 this metric shows a 54× amplification (54% ± 44% vs 1% frozen)
— a large but high-variance effect, not significant at n = 4 (§7.4). On long streams, V9’s
signature is consistency (3× lower variance than B1b), not higher mean. R2 and a §4.12
eval-time hybrid confirm functional orthogonality: parametric encodes typology, retrieval
delivers verbatim specifics (§6.3).
(C4) Honest positioning. EVAF is in the mechanistic-interpretability lineage (ROME,
linear-representation hypothesis, superposition) rather than a leaderboard system. Its
core deliverables are the selective consolidation gate and Test-Retest protocol — a
falsifiable harness for detecting parametric writes with replay-retention checks, distinguishing
optimization artefacts from rote fitting.
1


--- Page 2 ---
The corpus, generator, hyperparameter table, productive-recall dataset, and event-level
Test-Retest traces are versioned with the v9.0-frozen state used for all claims.

1

Introduction

Large language models deployed as long-horizon agents accumulate interaction histories that admit two
memory operations. Retrieval answers “what did the user say earlier?” at query time. Consolidation
asks which experiences should leave a persistent parametric imprint on how the agent writes and
responds, rather than remain lookup-only facts. We operationalize this question through measurable
parametric signatures (§4, §4.10–§4.11), not claims about cognitive identity. The 2024-2026 landscape
of LLM-memory systems has produced excellent retrieval architectures — Mem0, Zep, Letta/MemGPT
(Packer et al., 2023), HippoRAG (Gutiérrez et al., 2024), LIGHT (Tavakoli et al., 2025), EmergenceMem and
Mastra OM — that achieve 90-99% on verbatim- recall benchmarks such as LongMemEval (Wu et al., 2024)
and LoCoMo (Maharana et al., 2024). These systems work, and we do not propose to replace them. They
are also, by construction, retrieval systems: they store and look up; they do not consolidate into the model’s
parameters.
This paper takes up the orthogonal question: what does a selective parametric consolidation mechanism look like, and how does it behave across LLM scales? Specifically, we investigate whether a
parameter-efficient LLM can exhibit measurable signatures consistent with selective persona consolidation —
deciding which fragments of stream content are significant enough to admit to a slow parametric store — in a
way that admits falsifiable mechanistic measurements and derivable cross-scale rules. We position our work in
the lineage of mechanistic interpretability for memory (ROME (Meng et al., 2022); the linear-representation
hypothesis (Park et al., 2024); superposition (Elhage et al., 2022)) rather than as a competitor to retrievalaugmented systems on benchmarks they were designed for; we are explicit in §6.3 about retrieval and EVAF
operating on different timescales (query-time vs. cumulative persona-formation) and being architecturally
complementary, not zero-sum.
Why “persona consolidation” and not just “memory”? The classical complementary- learning-systems
(CLS) framework (McClelland et al., 1995; Kumaran et al., 2016) posits that a hippocampal fast store rapidly
binds arbitrary episodes while a neocortical slow store consolidates only the subset that interacts with prior
knowledge and emotional salience. In humans and other animals, this selective consolidation is what produces
personality and identity — the cumulative shape of the cortical store after a lifetime of experiences — not
what produces fact lookup (Eichenbaum, 2017; Schapiro et al., 2017). The biological filter is a dual gate: novel
content (high surprise) that is also emotionally tagged (high valence) is what shapes the slow store. EVAF
instantiates this dual-gate selectivity in an LLM at modern scale; the §4 mechanistic signatures are consistent
with selective parametric consolidation and a measurable persona-imprint signature — not a proof of identity
formation or query-time recall superiority. The closest prior LLM-side work that operationalises a CLS-like
pattern is Larimar (Das et al., 2024) (external content-addressable episodic memory, parameter-free at
write), FastSlow networks (Mujika et al., 2017), OML (Javed & White, 2019), and FearNet (Kemker &
Kanan, 2018); none of these contact LLM hidden-state geometry at modern scale, none implement a dual
valence-×-surprise gate, and none provide the mechanistic-evidence protocols that this paper centres.
Continual-learning baselines that do run on LLMs — naive LoRA updates (Razdaibiedina et al., 2023),
experience replay (Rolnick et al., 2019; Chaudhry et al., 2019), and EWC-style anchors (Kirkpatrick et al.,
2017; Zenke et al., 2017) — provide the algorithmic ingredients we re-use, but none characterise the
dynamics that govern the consolidation decision itself, nor provide a falsifiable protocol for
detecting when parametric writes occur and persist. This is the gap we address.
We propose EVAF (Echo-Valence Attractor Field), a framework for investigating selective parametric
consolidation in LLMs. EVAF treats the model’s prefix hidden √
state s(t) ∈ RP ×D √as a fast-timescale working
D−1
memory constrained to the per-token product manifold (S
( D))P ⊆ S P D−1 ( P D) — a hard-constraint
limit of the high-dimensional thin-shell concentration phenomenon (Vershynin, 2018), implemented by pertoken projection (§3.1) — and the LoRA-augmented attention weights θLoRA as a slow-timescale long-term
store. The two stores are coupled by a single probabilistic write gate
2


--- Page 3 ---


Pwrite (t) = σ kv (Vt − τv ) · σ ks (St − τs )
combining sample valence Vt (from an external oracle — see §3.3, §7.14) and epistemic surprise St (the
cross-entropy of the model on the sample under the current prefix). When both signals exceed their thresholds,
the sample is admitted to a polarized buffer and consolidated via a brief LoRA inner loop with experience
replay and an EWC-style anchor; otherwise it circulates on the working-memory attractor and is implicitly
forgotten. This dual gate, the specific spherical-attractor formulation, and the experimental protocol below
are, to our knowledge, original; the underlying components — LoRA (Hu et al., 2022), experience replay
(Rolnick et al., 2019), EWC (Kirkpatrick et al., 2017), and the FEP-grounded free-energy interpretation
(Friston, 2010) — are not, and we use them as established machinery.
To characterise whether parametric writes occur and are retained across past content — and to rule out
optimisation artefacts — we introduce a Test-Retest mechanistic protocol: for every consolidation event
we compute the surprise of the model on the buffer sample(s) and on a replay batch of past samples before
and after the LoRA inner loop, while freezing the working-memory state. A negative ∆Sbuf on the buffer is
consistent with a parametric write on the admitted material; a negative ∆Srep on the replay set — which is
included in the same inner-loop update — is a replay-retention (anti-forgetting) check on trained
past samples, not a held-out generalization measure (§7.5). The protocol is falsifiable: a counterfactual that
nulls the LoRA optimizer produces ∆S = +0.000 at machine precision across all events on both architectures
we evaluate.
Across counterfactual ablations (six variants), multi-seed robustness (five seeds for GPT-2, four for TinyLlama),
four base architectures spanning 124M to 7B parameters (GPT-2 with Conv1D attention, TinyLlama-1.1BChat with Llama-style RoPE, Qwen3-1.7B-Base with Qwen3 GQA, Mistral-7B-v0.3-Base with Mistral GQA),
two valence-oracle implementations (a heuristic and a DistilBERT-SST2 classifier), and a quantitative perturbation study (30 random-direction kicks at three intensities), EVAF exhibits four reproducible mechanistic
signatures:
1. Test-Retest parametric-write signature — across all four models, ∆Sbuf < 0 on every consolidation event of the seed=42 reference run (8/8 on GPT-2, 3/3 on TinyLlama-Chat, 6/6 on
Qwen3-Base, 5/5 on Mistral-Base; total 22/22 events with no exceptions), and at multi-seed the
mean per-event ∆Sbuf stays negative on every seed of every model (GPT-2 5-seed mean −2.89 ± 0.40,
TinyLlama-Chat 4-seed mean −9.75 ± 6.40). The same drop appears on the replayed past samples
(∆Srep tracks ∆Sbuf with the same sign and comparable magnitude) — consistent with a parametric
write plus replay-driven retention of past content (not held-out generalization; §7.5), not a cognitive
claim of “internalisation succeeded.”
2. Operational dynamical-freeze signature — post-consolidation, the working-memory trajectory
collapses from a ρ10 = −0.87 limit cycle (V5 baseline, 17.7◦ per step) to a fixed-point attractor: 7.73◦
on GPT-2, 2.39◦ on TinyLlama-Chat, 2.10◦ on Qwen3-Base, 4.08◦ on Mistral-Base. A z-score test
on the pre/post step-angle distribution separates V9 from all five counterfactual ablations on every
model (§4.3, §7.15; we do not claim a formal dynamical-systems phase transition).
√
3. Geometric lockdown — the spherical constraint holds at machine precision, ∥s∥2 − P D <
√
1.3 × 10−5 across all four models (max absolute norm deviation from P D, range 4.34 × 10−6 to
1.32 × 10−5 ) and survives 8-sigma random kicks with 100% basin return in Trec ≤ 2 steps, empirically
validating the FP32-round-off bound implied by the thin-shell concentration limit (§5.1).
4. Capacity- and maturity-aware re-anchoring — naive transfer of GPT-2-tuned hyperparameters
to larger models triggers V8-style catastrophic forgetting; on Mistral-7B-Base, the single-factor scaling
rule we initially proposed fails badly (∆S global =+12.57, i.e., the model worsens rather than improves
on the corpus). The empirical resolution requires three rules working in concert (§5): an adaptive
percentile τs that widens from Pct50 to Pct75−90 as the warmup-S distribution narrows; a multi-factor
Langevin anchor λreg with model-class–dependent capacity coupling; and a maturity-aware lrLoRA
that shrinks for deeply-pretrained Base models. The 4-model empirical audit precisely delimits each
rule’s regime of validity and identifies open theoretical questions (§5.5).
3


--- Page 4 ---
Figure 1: V5 limit-cycle working-memory attractor (the pre-consolidation trajectory). The hidden state
s(t) traces a closed orbit on the sphere ∥s∥2 = P D with ρ10 = −0.87 lag-10 anti-correlation and mean step
angle 17.7◦ . Post-consolidation, the same trajectory collapses to a fixed-point attractor (§4.3, Figure 2); this
collapse is the operational dynamical-freeze signature.
Contributions. Our contributions are:
(C1) Selective consolidation gate + Test-Retest protocol: the Pwrite dual-gate and paired before/after surprise
measurements, together with the operational dynamical-freeze z-score, constitute a falsifiable observation
toolkit for selective parametric consolidation that other continual-learning methods can adopt.
(C2) Cross-architecture and cross-scale validation over a ×57 scale range and four distinct attention architectures: GPT-2 (124M, Conv1D), TinyLlama-1.1B-Chat (Llama RoPE), Qwen3-1.7B-Base (Qwen3 GQA), and
Mistral-7B-v0.3-Base (Mistral GQA). The four mechanistic signatures (C1) reproduce on every model; the
scaling rules of (§5) that we derived from thin-shell concentration and EWC norm consistency are partially
predictive — they correctly forecast Qwen3-Base hyperparameters from base-model surprise statistics alone,
but fail quantitatively on Mistral-Base, where a maturity-aware revision is required.
(C3) Counterfactual and oracle-invariant evidence: a six-variant ablation matrix combined with a five-seed
(GPT-2) / four-seed (TinyLlama) robustness study, a DistilBERT-SST2 oracle re-run, and a thirty-trial
quantitative perturbation study. All reported numbers reproduce bit-for-bit from the v9.0-frozen state.
(C4) External-baseline matrix at multi-seed (§4.9). Five baselines at matched corpus and seed. Multi-seed
analysis shows V9 is best characterised as a selectivity-and-stability mechanism — lowest trigger-count
variance, healthy ∆S global distribution, and the only method preserving the operational dynamical-freeze
signature on every seed — rather than deeper per-event Test-Retest than LoRA baselines (per-event ∆S B
overlaps within 1σ on GPT-2).
(C5) Mechanism-to-persona transfer (§4.10–§4.12). Recognition: V9 beats Frozen in every cell; B3b RAG
wins on verbatim CE by design. Production: operational persona-imprint metric — 54× amplification
on GPT-2 short corpus (54% ± 44% vs 1% frozen; §4.11); on long streams V9’s signature is consistency (3×
lower variance than B1b), not higher mean. R2 discriminating-token tests and §4.12 eval-time hybrid confirm
parametric typology vs retrieval verbatim are functionally orthogonal (§6.3).
(C6) Second-order findings of independent interest: an “Oracle Precision Amplifier” effect (oracle precision
scales mechanism strength without changing direction); “Capacity-Dependent Mechanism Reweighting” (at
the measured ×1.91 LoRA capacity ratio the individual necessity of replay and EWC weakens while their joint
necessity is preserved); a “Basin Wall Geometry” surrounding the limit-cycle attractor; and a “Noise-Fitting
Trap” delimiting the validity of the Test-Retest parametric-write signal.
What we deliberately do not claim. EVAF should be interpreted as a falsifiable protocol for
studying selective parametric consolidation, not a complete theory of identity formation. We do not
4


--- Page 5 ---
beat retrieval-augmented systems on verbatim-recall benchmarks, and are not designed to. We report a
Sentence-BERT MiniLM RAG baseline at parity with our parametric methods in §4.10 and observe that on
a verbatim- recall recognition metric it produces lower cross-entropy than every parametric method including
V9 — this is structurally expected, since a metric that rewards exact-content retrieval is the natural strength
of retrieval. We position the parametric path (EVAF) and the retrieval path (RAG / Mem0 / Zep / LIGHT)
as operating on different timescales for different purposes: retrieval is the right tool for query-time
fact lookup; EVAF is the right tool for the cumulative shaping of an agent’s persona over a long interaction
history (§4.11, §6.3). We also do not claim a closed-form predictive scaling rule across all base models; the
Mistral-Base audit (§4.8, §5.4) reveals an lrLoRA co-revision supported by a single data point, which we report
as a conjecture and list as the single most decision-relevant follow-up experiment (§7.2). We list ten further
limitations in §7, including the end-of-stream evaluation-drift caveat that emerged from an extended-stream
stress test (§7.11).
The rest of the paper proceeds as follows. Section 2 surveys the closest prior art, with particular care for the
boundary between our contributions and the established CLS / replay / EWC / LoRA / memory-augmentedLLM literatures, including the 2024-2026 retrieval-augmented memory landscape (§2.4). Section 3 specifies the
framework. Section 4 presents the four mechanistic signatures and their counterfactual / robustness / crossmodel / cross-baseline variants, the persona-fact retention task that tests mechanism-to-task transfer (§4.10),
and the productive-recall free-generation experiment that demonstrates the long-timescale persona imprint
(§4.11). Section 5 develops the thin-shell scaling rule, the adaptive percentile τs , and the multi-factor Langevin
EWC scaling that together motivate the cross-model re-anchoring, and reports the 4-point empirical audit
that delimits each rule’s regime of validity. Section 6 discusses implications, the architectural-complementarity
argument between EVAF and retrieval on different timescales (§6.3), and a brief positioning note for the
cognitive-science audience. Section 7 lists eleven honest limitations.

2

Background and Related Work

2.1

Complementary Learning Systems (CLS) and dual-store memory

The complementary learning systems hypothesis (McClelland et al., 1995) posits that the brain maintains
two qualitatively distinct memory systems — a fast-binding hippocampal store and a slow-consolidating
neocortical store — that exchange information through replay during off-task periods. Subsequent work
refined the timing (Kumaran et al., 2016; Schapiro et al., 2017) and identified gating signals (novelty /
reward) that determine which hippocampal episodes get consolidated. EVAF is inspired by CLS but is not a
cognitive model: we do not claim to map our Prefix-state attractor to hippocampus or our LoRA-augmented
weights to cortex. We use the dual-store architectural pattern as a design constraint and reserve neuroscience
interpretation for §6 (Discussion).
Three recent ML systems share this architectural pattern. FearNet (Kemker & Kanan, 2018) trains a
dual-net for incremental classification with generative replay. OML (Javed & White, 2019) meta-learns
representations to minimise forgetting. FastSlow networks (Mujika et al., 2017) alternate fast-RNN inner
steps with slow-weight consolidation. None of these contact LLM-scale hidden-state geometry, none use the
dual-gate V × S structure, and none use the spherical-attractor working memory we develop in §3.1. The
closest prior work is Larimar (Das et al., 2024), which couples an LLM to an external episodic memory
addressable by content; Larimar is parameter- free at memory write (it stores keys in a separate non-LLM
memory matrix), while EVAF is parameter-internalising (LoRA edits travel into the model itself).
2.2

Continuous attractor networks and LLM hidden-state geometry

Continuous attractor networks (Wilson & Cowan, 1972; Burak & Fiete, 2009) realise short-term memory as a
ring or sphere of stable states; perturbations relax back to the manifold under linear-stability conditions on the
recurrent weights. The thin-shell concentration phenomenon
for high-dimensional
Gaussians (Vershynin, 2018)
√
√
— i.e., for x ∼ N (0, ID ) the norm ∥x∥ concentrates at D within O(1/ D) — is what makes a hard sphere
constraint approximately faithful √
at LLM-scale D ≳ 103 . We exploit this in §3.1 by
prefix
√ projecting each √
D−1
token onto its own sphere S
( D) — the per-token product manifold (S D−1 ( D))P ⊆ S P D−1 ( P D)
5


--- Page 6 ---
— at every step, and verify in §4.7 that the resulting working-memory trajectory exhibits an operational
dynamical-freeze signature (limit cycle → fixed point) when consolidation events complete — a signature
absent in pure attractor models without our gating layer.
The growing literature on LLM hidden-state geometry (Elhage et al., 2022; Gurnee & Tegmark, 2024; Park
et al., 2024) has documented that intermediate activations live near low-dimensional manifolds embedded in
a high-D ambient space. Our spherical projection is consistent with these findings and provides an analytic
hard-constraint counterpart to the empirical low-rank observations.
2.3

Continual learning: replay, EWC, and LoRA

The continual-learning literature is structured around three orthogonal mitigations of catastrophic forgetting
(French, 1999):
• Replay (Rolnick et al., 2019; Chaudhry et al., 2019): at each update, interleave the current sample
with past samples drawn from a buffer.
• Regularisation (Kirkpatrick et al., 2017; Zenke et al., 2017): add a penalty λ · Fisher·∥θ − θ0 ∥2
that anchors parameters near their pre-task value.
• Architecture (Rusu et al., 2016; Mallya et al., 2018): allocate fresh capacity per task, leaving prior
tasks’ parameters frozen.
LoRA (Hu et al., 2022) provides a parameter-efficient capacity-allocation substrate; recent LoRA-based
continual schemes (Razdaibiedina et al., 2023; Chitale et al., 2023) combine LoRA with replay or regularisation
in static task-sequence settings. EVAF integrates all three of replay, an EWC-style L2 anchor (we use a
uniform anchor in lieu of Fisher; §3.2), and LoRA capacity, but with two important departures: (i) the
trigger of a consolidation event is the dual gate V × S rather than a fixed task boundary; (ii) the trigger
fires at a fraction of a percent of stream steps (typical: 3–8 out of 210 turns on our corpus), making EVAF a
selectively-consolidating rather than continually-updating system. As we show in §4.4 (counterfactual C2) and
§4.9 (external baseline B1b), removing the gate and consolidating at every step yields shallow internalisation;
conversely, removing EWC (C2, B1a) yields collapse on TinyLlama-1.1B+. The combination of selective
gating + EWC anchor + replay is what we evaluate.
2.4

Memory-augmented LLMs (retrieval, structured memory, agent systems)

A parallel and currently-dominant line of work scales memory by externalising it. RAG (Lewis et al., 2020;
Izacard et al., 2023) and MemGPT / Letta (Packer et al., 2023) retrieve relevant past contexts at inference
time and inject them via the prompt. Memorizing Transformers (Wu et al., 2022) extend the attention
KV cache with a long-term store. Prompt-compression systems (Chevalier et al., 2023; Ge et al., 2024)
compress past contexts into a small number of soft tokens. The 2024-2026 wave of production long-termmemory systems has converged on multi-layer structured-memory architectures: Mem0 and Zep (Rasmussen
et al., 2025) maintain typed memory graphs (episodic / semantic / procedural) addressable by content and
updateable through agent tool calls; HippoRAG (Gutiérrez et al., 2024) adapts neuroscience- inspired
Personalised PageRank over a knowledge graph; LIGHT (Tavakoli et al., 2025) introduces a cognitivelyorganised multi-layer episodic / working / scratchpad memory and reports SOTA on the BEAM benchmark;
recent adaptive-RAG systems (EmergenceMem, Mastra OM) push LongMemEval (Wu et al., 2024) to
94-99% accuracy through dynamic-k retrieval with MMR diversification.
These systems are excellent at what they are designed for — query-time fact retrieval — but by
construction they leave the parameters of the LLM unchanged. They are external memory stores,
not consolidation mechanisms. EVAF asks a different question — which fragments of stream content should
shape the model itself, on the persona-formation timescale? — and is therefore positioned as architecturally
complementary to retrieval rather than competing with it on the recall benchmarks these systems were built
for (§6.3).
The strongest evidence for the complementarity is the productive-recall amplification of §4.11: an EVAFtrained model freely generates persona- specific keywords at a 4-seed mean 54× the frozen-base rate on
6


--- Page 7 ---
GPT-2 (54% ± 44% vs 1%; a large but high-variance effect, §7.4) on a prompt that contains no fact content
for a retriever to match. Retrieval cannot produce this signature even in principle, because the prompt is
below the retrieval-relevance threshold; the persona imprint lives in the parameters or it does not exist at all.
On the §4.10 recognition CE metric our Sentence-BERT MiniLM RAG baseline (B3b) does produce lower CE
than every parametric method including V9, as it should for any metric that admits exact-content retrieval;
this is the right outcome for the right metric, and we explicitly do not claim parametric superiority on recall.
We position EVAF and retrieval as operating on different timescales for different purposes: retrieval
is the query-timescale fact-lookup tool; EVAF is the persona-formation-timescale consolidation tool. We
implement a minimal eval-time hybrid in §4.12 (V9 stream + RAG at eval): it confirms the two paths are
orthogonal but shows naive concatenation is not sufficient fusion — write-time routing remains the natural
follow-up (§6.3).
2.5

Mechanistic interpretability for memory

A small but growing body of work probes how LLMs encode and recall factual knowledge (Meng et al., 2022;
Geva et al., 2022; Hernandez et al., 2024) and how that encoding updates under fine-tuning (De Cao et al.,
2021; Hartvigsen et al., 2023). These works diagnose which parameters hold a given fact and how an edit
propagates. EVAF operates at a different level: we do not localise individual facts but instead instrument the
dynamics of the consolidation decision through the Test-Retest protocol (∆Sbuf and ∆Srep , §3.4) and the
working-memory dynamical-freeze signature (z-score on the step-angle distribution, §4.3). The two tool-sets
are complementary: mechanistic-interpretability work tells us where a fact ends up after a write; EVAF tells
us whether and when the system decides to write.
2.6

Summary: what is and is not new in EVAF

Element

Established

Our contribution

Dual-store CLS framing
Spherical hard-constraint on a
prefix state

(McClelland et al., 1995)
thin-shell limit (Vershynin,
2018)

LoRA

(Hu et al., 2022)

Replay

(Rolnick et al., 2019)

EWC-style anchor

(Kirkpatrick et al., 2017)

Dual gate V × S
Test-Retest protocol

—
—

Operational dynamical-freeze
z-score
Multi-factor λreg scaling rule

—

Persona-fact retention as a
downstream evaluation of
mechanism→task transfer

—

(architectural inspiration only)
the projection on every step and
its falsifiable lock signature
(§4.7)
parameter-efficient capacity
substrate
replay sampling from past
stream excluding current buffer
uniform-Fisher L2 anchor with
scaling rule §5
new (Pwrite in §3.3)
new (mechanistic-evidence
harness, §3.4)
new (working-memory freeze
signature, §4.3)
new (§5, §11.5 of math
framework)
new in the sense of being our
specific design (§4.10); the
dataset family (PersonaChat) is
well-known (Zhang et al., 2018)
new: generative-level probe
consistent with selective
consolidation; not a cognitive
identity claim

—

—
Productive-recall
persona-imprint metric
(operational: free generation
from content-free prompt; §4.11)

7


--- Page 8 ---
Element

Established

Our contribution

Reframing of dual-gate
—
selectivity as a persona-relevance
filter rather than a generic
surprise filter

new framing, motivated by
§4.11 amplification and §6.3
complementarity argument

The remaining sections develop the framework (§3), report the experimental findings (§4), formalise the
scaling rules (§5), and discuss implications and honest limitations (§6).

3

The EVAF Framework

EVAF couples a √
fast working-memory
store s(t) — a low-rank prefix constrained to the per-token product
√
manifold (S D−1 ( D))P ⊆ S P D−1 ( P D) — to a slow long-term store θLoRA — rank-r adapter weights
on the attention modules — via a probabilistic write gate Pwrite (Vt , St ). §3.1 specifies the prefix dynamics;
§3.2 the LoRA inner loop with replay and EWC anchor; §3.3 the dual gate; §3.4 the Test-Retest mechanistic
protocol that decides whether a write succeeded.
3.1

System 1 — Prefix dynamics on a high-dimensional sphere

Let s(t) ∈ RP ×D denote the trainable prefix at step t, with P the prefix length and D the hidden width of
the base model. The prefix is initialised s(0) ∼ 0.02 · N (0, IP D ) and projected onto the sphere ∥s∥2 = P D.
The base model is frozen; gradients flow only into s via the cross-entropy loss

St = −


1 X
log pθ yt,i | yt,<i , s(t) ,
|yt | i

(3.1)

evaluated on the current input yt with the prefix prepended. We refer to St as the epistemic surprise at
step t.
A single update step combines three terms:


s(t + 1) = Π(S D−1 (√D))P s(t) + α −∇s St + λcon s(t) − sEMA (t)
,

(3.2)

with a gate-modulated
exponential moving average sEMA (t) = (1 − ρt ) sEMA (t − 1) + ρt s(t) whose rate

ρt = ρ σ k (St − τs ) tracks the surprise gate (base rate ρ; the anchor slides quickly when surprise is high and
is nearly frozen when √
surprise is low), and Π the per-prefix-token projection that
√ rescales each of√the P
token vectors to radius D, so the joint state lies on the product manifold (S D−1 ( D))P ⊆ S P D−1 ( P D).
The first term descends along the surprise gradient; the second is a repulsive (contrastive) inertia term
that pushes the state away from its own slow EMA, keeping the pre-consolidation working memory in a
sustained limit cycle rather than collapsing it (an EMA-sign control, §4.3, shows that flipping this term to an
attractive “pull toward the EMA’ ’ removes the limit cycle and destroys the freeze signature). The projection
enforces the spherical hard constraint. No fine-tuning is performed on either the prefix or the base model
during step (3.2); changes to base parameters arrive only via the LoRA inner loop in §3.2 below.
The spherical constraint is faithful because of the thin-shell concentration of measure in high dimension
√
(Vershynin, 2018): an unconstrained
Gaussian initialisation in RP D already concentrates
within O(1/ P D)
√
√
of the sphere of radius P D. The projection Π therefore corrects an O(1/ D) residual rather than a
leading-order deformation; §5.1 derives a quantitative bound on the surviving deviation.
8


--- Page 9 ---
3.2

System 2 — LoRA + experience replay + EWC anchor

We attach LoRA adapters (Hu et al., 2022) with rank r = 32 to the attention projections of the base model
(q/k/v/o or, for GPT-2, the Conv1D analogues). When a consolidation event is fired at step t∗ (see §3.3 for
the trigger condition), the inner loop runs Ninner = 5 AdamW steps on the joint loss
LLoRA (θ) =

X

1
CE y | s(t∗ ), θ + λreg ∥θ − θ0 ∥22 ,
|B ∪ R|

(3.3)

y∈B∪R

where B is the polarized buffer (admitted samples since the previous event; capacity |B| = 4), R is a
K = 4-sample experience replay drawn uniformly from the global stream of all past inputs excluding B,
and θ0 is the LoRA weights at adapter-attach time. The L2 anchor λreg ∥θ − θ0 ∥22 approximates an EWC
penalty (Kirkpatrick et al., 2017) with a uniform Fisher; §5.3 derives the scaling rule that determines λreg as
a function of base model. The prefix s(t∗ ) is held fixed during the inner loop — gradients update only LoRA
weights — which decouples the slow store update from the fast store dynamics.
After the inner loop, the buffer is cleared. The dynamics in (3.2) resume with the now-modified base model
(the LoRA delta is added to the frozen base through the standard PEFT path).
3.3

Dual gate — what gets admitted to the buffer

Each input yt carries a valence Vt ∈ [0, 1] from an external oracle — EVAF is an oracle-conditioned
consolidation protocol, not a self-contained valence mechanism (§7.14). In the main experiments we use a
calibrated lexical-affect heuristic; in the Patch-A robustness study, a DistilBERT-SST2 classifier. Epistemic
surprise St comes from (3.1). The dual gate is


Pwrite (Vt , St ) = σ kv (Vt − τv ) · σ k (St − τs ) ,

(3.4)

and yt is admitted to the buffer iff Pwrite (Vt , St ) > τwrite = 0.55. The product form requires both gates to
open — a sample with only high valence or only high surprise is rejected. We show in §4.4 that ablating either
gate factor degrades the mechanism in the expected direction: surprise-only gating (B2) yields catastrophic
forgetting on TinyLlama; valence-only gating (omitted for brevity) yields under-firing.
A consolidation event triggers when the buffer reaches its capacity |B| = 4; the buffer is then frozen as input
to (3.3) and a new buffer begins to accumulate.
3.4

Test-Retest — protocol for parametric-write detection

To characterise whether consolidation produces a parametric write with a replay-retention (antiforgetting) check rather than merely consuming compute, we instrument every consolidation event with a
paired before/after measurement. Let θpre and θpost denote the LoRA weights immediately before and after
the inner loop. We compute four quantities:
∆StB∗ =


1 X
S(y; s(t∗ ), θpost ) − S(y; s(t∗ ), θpre ) ,
|B|

(3.5a)


1 X
S(y; s(t∗ ), θpost ) − S(y; s(t∗ ), θpre ) ,
|R|

(3.5b)

y∈B

∆StR∗ =

y∈R

∆S global =

t1
T
1 X
1 X
St −
St ,
T − t1 t=t
t1 t=1
1

9

(3.5c)


--- Page 10 ---
with the prefix held fixed at s(t∗ ) for (3.5a, b), and t1 the first consolidation (LoRA-trigger) step, St the online
per-step surprise (3.1), and T the stream length. The first quantifies whether the inner loop reduced surprise
on the buffer. The second measures the change on the replay set R; because R is part of the same inner-loop
batch (B ∪ R, EQ 3.3), ∆S R is a replay-retention (anti-forgetting) check on trained past samples,
not a held-out generalization measure (§7.5). The third compares the model’s online stream surprise in the
post-trigger window (t ≥ t1 ) against its pre-trigger baseline (t < t1 ) — an online stream-level baseline-shift
/ collapse detector, not a fixed held-out final-vs-initial corpus metric: a healthy run gives ∆S global < 0
with small magnitude, while a positive or extreme-negative value indicates catastrophic forgetting / runaway
memorisation.
The fourth signature is the operational dynamical-freeze signature: we compute the distribution of
per-step angles θt = ∠(s(t), s(t − 1)) in the pre- and post-event windows, and report a z-score test on whether
the post window has significantly smaller angles (the working-memory trajectory freezes onto a fixed point
once consolidation completes). We do not claim a formal dynamical-systems phase transition (§7.15). §4.3
details this signature; in brief, z > 3 across all 8 GPT-2 V9 events and z > +4 on TinyLlama, Qwen3, and
Mistral; with z < +1 on every counterfactual ablation where the LoRA inner loop is nulled (C0/C3) or
replayed with random tokens (C1).
3.5

Implementation summary

The system is fully specified by the four ingredients above plus the model-specific hyperparameter triple
(τs , λreg , lrLoRA ) that §5 derives from the base model’s surprise distribution and LoRA parameter count.
Algorithm 1 (Appendix A) gives the end-to-end pseudo-code; the v9.0-frozen source state pins all numerical
constants and reproduces the results in §4 bit-for-bit.

4

Experiments

We organise the experimental section around the four mechanistic signatures identified in §1 and around
the question “do those signatures translate to useful task behaviour?”. §4.1-§4.4 establish the signatures on
GPT-2; §4.5-§4.7 audit their robustness (multi-seed, oracle invariance, perturbation stability); §4.8 scales to
four models from 124M to 7B parameters; §4.9 compares against five external baselines at multi-seed; §4.10
reports a downstream persona-fact task.
4.1

Setup

Corpus. 210 sentences drawn from a controlled 10-category lexicon covering mixed valence and surprise.
The same corpus is used for §4.1-§4.9; §4.10 uses a separately-generated persona-fact corpus (see §4.10).
Base models. GPT-2 124M (Conv1D attention), TinyLlama-1.1B-Chat (Llama RoPE), Qwen3-1.7B-Base
(Qwen3 GQA), Mistral-7B-v0.3-Base (Mistral GQA). LoRA is attached to attention projections in each case
(q, k, v, o or their Conv1D analogues), with rank r = 32.
Hyperparameters. Model-specific (τs , λreg , lrLoRA ) values follow the rules of §5 — explicitly, GPT-2 uses
(6.0, 10−3 , 5 · 10−3 ), TinyLlama-Chat uses (11.5, 10−2 , 5 · 10−3 ), Qwen3-Base uses (4.1, 5 · 10−4 , 5 · 10−3 ),
Mistral-Base uses (5.0, 5 · 10−2 , 5 · 10−4 ). The remaining hyperparameters are model-invariant and listed in
Appendix A.
4.2

Test-Retest parametric-write signature on GPT-2

We instrument every consolidation event with the Test-Retest measurement (§3.4 EQ 3.5a-b) and report
event-level ∆S B and ∆S R . On the GPT-2 seed=42 reference run V9 fires 8 consolidation events; the mean
per-event ∆S B = −3.42 and ∆S R = −2.93, both negative on every event and positively correlated in
magnitude (r = 0.91). The stream-level ∆S global = −0.564 is healthily negative (well below zero without
crossing the V8-style collapse threshold of ≤ −3).
10


--- Page 11 ---
Figure 2: V9 6-panel ablation matrix on GPT-2 (seed=42). Each panel reports one mechanistic signature
across the V9 / C0–C4 counterfactual variants; V9 is the only configuration that passes all four signatures
jointly.
Counterfactual ablations C0 (no optimiser step), C1 (random-token replay), C2 (no EWC anchor), C3 (zero
inner steps), and C4 (no replay set) each fail at least one of the per-event ∆S signs or the freeze test; C2
specifically produces a deeper per-event ∆S B but flips the stream-level ∆S global from V9’s −0.56 to +0.05,
erasing the net consolidation margin.
Figure
2
(the
6-panel
ablation
matrix)
ablation_runs/ablation_seed42.png make this concrete.
4.2.1

and

the

corresponding

table

in

Held-out parametric transfer (∆S H )

The replay term ∆S R above is a replay-retention (anti-forgetting) check: replay samples enter the LoRA inner
loop alongside the buffer (B ∪ R, EQ 3.3), so a negative ∆S R certifies retention of trained past samples, not
generalization to unseen data. To test whether a consolidation event transfers beyond the trained batch, we
add a held-out set H of 30 sentences spanning the same ten semantic clusters as the stream but disjoint from
it (verified programmatically), so H never enters the buffer or the replay pool. We report the parameter-only
change ∆S H (θ) — held-out surprise under the final LoRA minus the initial LoRA, with the prefix held
at its initial value — which isolates the parametric write from working-memory drift. The probe captures
11


--- Page 12 ---
CPU snapshots of the LoRA and prefix during the run and scores H offline, so the canonical V9 metrics
(ntrig , ∆S global , ∆S B ) reproduce bit-for-bit (ablation_runs/dsh_probe/). A per-event in-context ∆S H
is recorded only as a diagnostic: on the higher-capacity model it is dominated by prefix-induced surprise
inflation (pre-event S H rises to 22–40 nats and the inner loop merely restores it toward baseline; cf. §7.11),
so we do not read it as held-out generalization.
On GPT-2, only the full system produces a parametric write that lowers held-out surprise: ∆S H (θ) = −1.15
nats (−16% of the initial S H = 7.03), against the much larger trained-batch effects ∆S B = −3.42 and
∆S R = −2.93. Every ablation that still trains on the same stream instead raises held-out surprise — random
replay (C1) +1.70 (+24%), no-EWC (C2) +0.51 (+7%), no-replay (C4) +2.42 (+34%) — while the no-write
controls leave it unchanged (C0, C3 ≡ 0). Exact-string leakage of H is ruled out by construction; the ablation
contrast further argues that the effect is not mere exposure to the training stream, since variants trained
on identical data do not lower ∆S H . On TinyLlama-Chat the parameter-only effect is near-neutral (V9
+0.81, +8%; all writing variants within +2–8%), so held-out parametric transfer at a fixed reference prefix is
model-dependent (§7.5).
Variant (GPT-2)

∆S H (θ)

% of S H

per-event nH <0

V9 (full)
C0 No-Update
C1 Random-Replay
C2 No-EWC
C3 Zero-Inner
C4 No-Replay

−1.15
0.00
+1.70
+0.51
0.00
+2.42

−16%
0%
+24%
+7%
0%
+34%

8/8
0/11
9/11
8/8
0/11
7/10

4.3

Operational dynamical-freeze signature (post-consolidation step-angle collapse)

We sample the per-step angle distribution θt = ∠(s(t), s(t − 1)) on a 30-step window preceding each
consolidation event (“pre-event”) and a 30-step window immediately following (“post-event”). The pre-event
window is consistent with V5 baseline statistics (mean θ ≈ 17.7◦ ); the post-event window collapses to
θ < 10◦ on every V9 event (7.73◦ on GPT-2 mean, 2.39◦ on TinyLlama-Chat, 2.10◦ on Qwen3-Base, 4.08◦ on
Mistral-Base). The pre-post comparison yields a single-sided z-score on the angle reduction; for V9 events
z > 3 across all GPT-2 events and z > +4 across the other three models.
Ablations C0/C3 (no LoRA update path) produce z < 1; ablation C1 (random- token replay) produces
a non-zero z but in the wrong direction (∆θpost > ∆θpre , i.e., the trajectory becomes more dispersed
after a random-replay event). The z-score is therefore a clean discriminator that survives ablation. We use
“operational dynamical-freeze” to denote this empirical signature; we do not claim a formal bifurcation or
Lyapunov-based phase transition (§7.15).
EMA-sign control. The consolidation term in EQ 3.2 is repulsive — it pushes the working-memory state
away from its slow EMA. To test whether this sign is what produces the freeze, we re-ran the V9 seed=42
reference with the term flipped to an attractive “pull toward the EMA” (−λcon ), holding the seed, corpus,
and every other hyperparameter fixed. The attractive variant collapses the trajectory to a near-fixed point
before any consolidation (pre-event step angle 20.2◦ → 3.2◦ ), so there is no limit cycle left to freeze: the
freeze z-score falls from +6.07 to −1.73 and the gate fires only 2 times (vs 8). Per-event surprise still drops
under both signs (∆S B < 0, driven by the LoRA inner loop), confirming that the freeze signature comes
from the repulsive working-memory dynamics rather than from surprise reduction alone. The probe script
and numbers are in ablation_runs/ema_probe/.
4.4

Six-variant counterfactual ablation matrix

The ablation matrix (V9 plus five counterfactuals C0–C4), each disabling exactly one mechanism, is summarised
in Table 4.4 (full numbers in ablation_runs/ablation_table.md). Every ablation produces a qualitative
degradation on at least one of the four signatures of §1; no ablation produces a deeper outcome on V9’s
12


--- Page 13 ---
Figure 3: V9 multi-seed robustness on GPT-2 (5 seeds, error bars). All four §1 mechanistic signatures hold
on every seed; magnitudes are seed-stable within 1σ.
full criterion. We note in particular: C2 (no EWC) achieves the largest per-event ∆S B depth but flips the
stream-level ∆S global from V9’s healthy −0.56 to +0.05, erasing the net consolidation margin — a clean
demonstration that EWC is not a “more depth is better” knob but a stability constraint. (The most positive
∆S global is C1 random-replay at +0.59.)
4.5

Multi-seed robustness

§1 reports five-seed GPT-2 / four-seed TinyLlama-Chat robustness. The four qualitative invariants (sign
of ∆S B , sign of ∆S global , z > 0 post-event freeze, δ-bound on geometric lock) hold on every seed of every
model. The relevant figures are ablation_runs/multiseed_v9.png (5-seed V9 on GPT-2) and §4.9 below
(combined 5-seed GPT-2 / 4-seed TinyLlama-Chat at multi-method).
4.6

Oracle invariance

Replacing the heuristic-lexical valence oracle with a frozen DistilBERT- SST2 classifier preserves all four
§1 signatures, with a single quantitative shift: the post-event step angle deepens further (7.73◦ → 1.47◦ on
GPT-2), as predicted by an “oracle precision amplifier” analysis detailed in the supplementary material. The
mechanism direction is preserved; the strength scales with oracle precision.
4.7

Quantitative basin stability

√
The geometric lock ∥s∥2 − P D survives 30 random-direction kicks applied at three intensities (1σ, 4σ,
8σ of the initial state magnitude). Recovery time Trec to the locked manifold is ≤ 2 Euler steps on every
trial; the basin wall geometry (the Trec vs. kick-magnitude curve) is linear over the tested range. See the
supplementary material for full numbers and Figure 5.
13


--- Page 14 ---
Figure 4: Oracle-invariance on GPT-2. Replacing the heuristic-lexical valence oracle with a frozen DistilBERTSST2 classifier preserves all four §1 signatures; the post-event step angle deepens from 7.73◦ to 1.47◦ (Oracle
Precision Amplifier effect).
4.8

Cross-architecture and cross-scale validation

An initial 2-model check and its extension (adding Qwen3-Base + Mistral-Base) together provide a 4-point
cross-model audit. The four §1 signatures reproduce on every model:
Model

|∆S B |

|∆S global |

θpost

max δ

GPT-2 124M
TinyLlama-Chat
1.1B
Qwen3-Base 1.7B
Mistral-Base 7B

3.42
18.51

0.564
1.649

◦

7.73
2.39◦

4.34 · 10−6
1.32 · 10−5

4.95
4.12

0.19
1.83

2.10◦
4.08◦

1.32 · 10−5
1.28 · 10−5

with hyperparameters fixed by the §5 rules. Naive transfer of GPT-2-tuned (τs , λreg ) to a larger model
produces V8-style collapse (e.g., TinyLlama with τs = 6.0 gives ∆S global = +3.38, step angle 25.13◦ exceeding
14


--- Page 15 ---
Figure 5: Perturbation-robustness. 30-trial random-direction kicks at 1σ, 4σ, 8σ of the initial state magnitude;
geometric lock recovers within ≤ 2 Euler steps on every trial.
the V5 baseline); the §5.2 P-band τs rule + §5.3 multi-factor λreg rule (or §5.3’ Chat- Base correction) is
what makes the four-point validation work.
Figure 7 shows the per-model V9 signatures side-by-side, and Figure 8 shows the hyperparameter-vs-scale
relationships from which §5 was derived.
4.9

External baselines (single-seed and multi-seed)

We compare V9 against five baselines drawn from established continual- learning and memory-augmented-LLM
categories:
• B1a Naive-LoRA same-compute: gate off, V9-matched trigger schedule, λreg = 0. Compute parity
ensured by construction.
• B1b Naive-LoRA 1-step continual: every-step LoRA update, no gate, no EWC.
• B2 Experience Replay (Rolnick 2019): surprise-only gate, no EWC.
• B3a FIFO-Context: last-K=8 plain prefix context, no parameter update.
• B3b RAG-Context: top-K=8 Sentence-BERT MiniLM retrieval, no update.
We first report the seed=42 single-seed comparison, then extend V9/B1a/B2 to 5 seeds on GPT-2 and
4 seeds on TinyLlama-Chat. The multi-seed analysis reveals that some of the seed=42 baseline-failure
claims (TinyLlama B2 catastrophic forgetting, TinyLlama B1a collapse) are partially seed-dependent; the
multi-seed headline reframing is:
V9 is best characterised not as deeper than the LoRA baselines in per-event Test-Retest
(the within-event ∆S B for V9 overlaps with B1a/B2 within 1σ on GPT-2 multi-seed: V9
−2.89 ± 0.40, B1a −2.97 ± 0.47, B2 −3.17 ± 1.24), but as the only method that simultaneously:
(i) maintains the ∆S global in a healthy negative range with seed-stable variance (GPT-2:
−0.70 ± 0.48; TinyLlama: −0.57 ± 0.93), (ii) preserves the post-event dynamical freeze on
15


--- Page 16 ---
Figure 6: Cross-model comparison (4 panels). Same four §1 signatures across GPT-2 124M, TinyLlama-Chat
1.1B, Qwen3-Base 1.7B, Mistral-Base 7B — all hold under scale-aware (τs , λreg , lrLoRA ) re-anchoring (§5).
every seed of every model, and (iii) keeps the trigger count tight (7.8 ± 1.5 on GPT-2 vs
19.8 ± 16.3 for B2 with one 49-trigger runaway seed; freeze z-score: V9 +3.29 ± 3.80 vs B2
+642.59 ± 1428.25, the latter dominated by the runaway seed and signalling unconstrained
gating). We use the framing selectivity and stability rather than dominance for these
results.
The full multi-seed numbers and per-claim “robust / weakened / newly supported” tagging is in the
supplementary material. Figure 9 shows the 2×4 panel matrix of ∆S B , ∆S global , post-event step angle, and
z-score across both models and all six methods.
4.10

Persona-fact retention — does mechanism translate to task?

Scope. Sections §4.10–§4.12 are a downstream persona case study, not the paper’s headline claim.
The main claim is the parametric-consolidation protocol and mechanistic signatures in §3–§4.9; the
recognition/productive-recall metrics below are designed probes that test whether those signatures transfer
to one controlled application (§7.7).
To test whether V9’s mechanistic advantages produce a downstream performance signal beyond the withincorpus Test-Retest measurement, we designed a persona-fact retention task that interleaves 30 specific
persona-facts (5 personas × 6 fact-templates with unique numeric and locale signatures) with 30 or 150
generic-knowledge filler turns, forming sequential streams of 60 or 180 turns respectively. After processing the stream, we measure the per-token cross-entropy on each persona-fact under a minimal prompt
("About {persona}: "), without recapitulating the fact in the prompt.
16


--- Page 17 ---
Figure 7: 4-point V9 signature matrix. Per-model panels for ∆S B , ∆S R , ∆S global , post-event step angle,
max freeze z, geometric-lock max deviation.

Figure 8: hyperparameter-vs-scale scaling: τs vs μS (§5.2 P-band rule), λreg vs nLoRA (§5.3 multi-factor),
lrLoRA vs μS (§5.4 conjecture). The audit defines each rule’s regime of validity.
Four methods are compared: Frozen (no parameter updates, baseline lower bound), V9 (full EVAF), B1b
(matched-architecture naive 1- step LoRA), and B3b (Sentence-BERT MiniLM RAG with top-K=4 retrieval).
Each (method, model, stream-length) cell is run with seeds {1, 2, 3, 42}, giving 64 runs total. Hyperparameters
are unchanged from §4 — we deliberately do not re-tune for this task.
Mean post-training persona-fact CE (lower is better):

Stream

Model

Frozen

V9

B1b

B3b

V9 mean
triggers

60-turn

GPT-2 124M

8.21 ± 0.55

5.75 ± 0.58

6.51 ± 0.12

2.18 ± 0.00

2.0

17


--- Page 18 ---
Stream

Model

Frozen

V9

B1b

B3b

V9 mean
triggers

60-turn

TinyLlamaChat 1.1B
GPT-2 124M
TinyLlamaChat 1.1B

12.10 ± 1.71

11.66 ± 2.13

6.96 ± 1.40

4.39 ± 0.00

0.2

8.62 ± 2.09
10.28 ± 1.01

7.06 ± 0.96
9.29 ± 0.63

6.63 ± 1.15
7.75 ± 1.66

2.18 ± 0.00
4.39 ± 0.00

4.2
1.2

180-turn
180-turn

We highlight the parametric winner (Frozen / V9 / B1b only) in bold; B3b is reported separately because it
operates by exact retrieval at eval time. Figure 10 shows the four panels as a bar chart; Figure 11 plots V9
vs B1b as a function of stream length.
4.10.1

What this experiment shows

1. Mechanism → task signal. V9 beats Frozen in every cell of the table — including TinyLlamaChat 60-turn (where V9 fires on average only 0.2 times across seeds — i.e., on 1 of 4 seeds), and
the improvement scales with trigger count. This is the cleanest result of §4.10: the mechanistic
improvements measured by ∆S in §4.2 propagate to a task CE metric measured on a separatelygenerated prompt that did not appear during training.
2. V9 best on short GPT-2 streams. V9’s 2 selective writes on the 60-turn GPT-2 corpus produce
a 5.75 ± 0.58 CE that is lower than B1b’s 6.51 ± 0.12 at no 1σ overlap. The matched- architecture
every-step naive baseline is meaningfully worse.
3. On longer streams the parametric advantage shifts to B1b. With 180 turns of corpus B1b
accumulates ∼ 180 naive LoRA updates and memorises individual fact tokens more aggressively
than V9’s ∼ 4 selective updates × 5 inner steps. On GPT-2 the two methods overlap within 1σ
(7.06 ± 0.96 vs 6.63 ± 1.15); on TinyLlama-Chat B1b leads (9.29 ± 0.63 vs 7.75 ± 1.66). This is not a
defeat for V9’s design principles — V9 is intentionally conservative, paying for stability — but we
report it at face value.
4. B3b RAG dominates by design. Sentence-BERT MiniLM retrieves the exact persona-fact
verbatim at eval time, producing CE 2.18 on GPT-2 and 4.39 on TinyLlama-Chat (zero std because
the retrieval is seed-independent). The persona-fact metric rewards verbatim production, so
retrieval is a near-zero-effort lower bound; we report this as a reference ceiling for retrieval-augmented
inference rather than a “best parametric method”.
5. Minimum stream length for V9 to trigger. On TinyLlama-Chat 60-turn V9 mean triggers
= 0.2 (one trigger across four seeds); on 180-turn the same hyperparameters give mean 1.2 (4/4 seeds
trigger at least once). This is a discovered design constraint of V9’s selectivity — not a bug — and is
consistent with the §5.2 P-band rule that admits roughly half the surprise distribution into the gate.
6. End-of-stream evaluation drift on extreme stream lengths (negative result). We additionally ran a 600-turn smoke test on GPT-2 seed=42 (V9 CE 8.47 at 4 triggers, L2 from LoRA init
1,536; B1b CE 8.02 at 600 updates, L2 ∼ 30,000 extrapolated). Both methods worsen relative to their
60- and 180-turn results; we interpret this as a universal end-of-stream prefix-drift effect rather than
a method-specific issue, and report it as such in §7.11. The interesting V9-vs-B1b gap that survives
the stress test is the 20× smaller LoRA L2 drift and ∼ 150× smaller optimisation-step count at
comparable end-of-stream CE — a compute / parameter-efficiency story, not a verbatim-recall-quality
story. The cleanest follow-up evaluation that would expose V9’s selectivity advantage is per-event
evaluation at the training-time prefix state (closer in spirit to Test-Retest measurement), not endof-stream evaluation; we scope this for the revision.
18


--- Page 19 ---
Figure 9: external-baseline comparison: 2 × 4 panels (GPT-2, TinyLlama-Chat × ∆S B , ∆S global , step◦
postAll, max freeze z). V9 is the only method on the “selectivity + stability” frontier — competitive depth,
seed-stable variance, bounded trigger count.
4.10.2

What this experiment does not show

We deliberately do not claim that V9 dominates all baselines on this task. The persona-fact CE metric
structurally rewards verbatim recall, which is the natural strength of retrieval (B3b) and aggressive every-step
learning (B1b at long stream length). The honest message is that V9 is the best zero-retrieval-latency
parametric method in the stream regimes where its selectivity gate fires, and that mechanism
improvements (§4) propagate to a task metric (§4.10) under controlled prompting.
Statistical tests (paired Wilcoxon + paired t,
n
=
4 seeds;
full table in
w4_runs/aggregates/w4_stat_tests.md): on GPT-2 60-turn, V9 vs Frozen recognition CE is significant by paired t-test (∆ = −2.46 nats, p < 0.001); V9 vs B1b is borderline (∆ = −0.76, p = 0.058). B3b
dominates all parametric methods (p < 0.01 on t-test). Wilcoxon signed-rank at n = 4 has limited resolution
(minimum p = 0.125); we report both tests throughout §4.10–§4.12.
Reviewer-anticipated follow-up tasks (LongMemEval, LoCoMo) are listed in §7.7 (Limitations) as scoped for
the revision cycle.
4.11

Productive recall — the persona-imprint signature

Terminology. We use persona-imprint as an operational term for productive recall under content-free
prompting — the fraction of free generations (from "About {persona}: with no fact content in the prompt)
that contain persona-characteristic keywords (Appendix B). It is a behavioral / distributional metric,
not a claim that the model has formed a cognitive identity or stable self-concept.
§4.10 measures recognition: whether the trained model assigns low surprise to a held-out persona-fact when
prompted with that persona’s name. This is a useful but limited probe — a model could plausibly lower CE
on a verbatim completion without shifting its open-ended generative distribution. The persona-imprint
metric targets that gap: whether stream-trained parameters bias free generation toward persona-typical
content when the prompt carries only a persona name, with no retrievable fact string.
We test this by free generation from the parameters of the trained model. For each (method,
model, seed) cell of §4.10 we take the final model state (post-stream prefix + LoRA), prompt with
"About {persona}: {persona} " for each of the five personas, and freely sample 10 completions of
19


--- Page 20 ---
Figure 10: Downstream persona case study: recognition CE (§4.10). Four panels: GPT-2 and TinyLlama-Chat
× 60-/180-turn streams; mean ± std over 4 seeds. V9 beats Frozen in every cell, while B3b RAG wins on
verbatim recall by construction.
40 new tokens at T = 0.8. We then count how many of the persona’s characteristic keywords (climbing,
Dolomites, persona-specific language, persona-specific instrument, persona-specific city, etc.; see Appendix B
for the full keyword set) appear in each generation. Three metrics are reported per cell:
• Hit rate — fraction of free generations containing at least one persona-specific keyword (broad:
shared + discriminating).
• Mean specific-keyword count — average number of distinct persona- specific keywords per
generation.
• Discriminating selectivity (R2) — for the 3 truly-unique discriminating tokens per persona
(Mandarin/Reykjavik/cello for Alice; Portuguese/Marrakech/oboe for Bob; etc.), the rate at which
the model emits this persona’s discriminating tokens versus another persona’s discriminating tokens
(cross-persona contamination). This isolates whether the parametric path encodes typology (shared
themes) versus verbatim specifics (persona-discriminating tokens).
Frozen-base is the no-imprint lower bound (the parameters are unchanged, so any keyword appearance is
by base-model prior). B3b RAG is included here even though its retrieval channel does not match against
the content- free prompt — it serves as the retrieval-control row showing what an otherwise-identical RAG
system contributes in the productive-recall regime where retrieval cannot help.
Persona-imprint amplification (4-seed multi-seed, {1, 2, 3, 42}, 10 gens × 5 personas = 50 free
generations / seed; full table in w4_runs/aggregates/persona_gen_table.md):
20


--- Page 21 ---
Figure 11: Stream-length scan (V9 vs B1b recognition CE). On long streams V9’s downstream signature is
consistency and lower drift, not higher mean performance.
Short corpus (60-turn):

Method

Model

Hit rate

Mean specific
keywords

Amplification
(vs Frozen)

n_triggers /
LoRA L2

Frozen
V9
B1b
B3b (ablated;
no retrieval
ctx)
Frozen
V9

GPT-2
GPT-2
GPT-2
GPT-2

1% ± 1.2%
54% ± 44%
81% ± 14%
26% ± 6%

0.01 ± 0.01
0.70 ± 0.62
1.31 ± 0.42
0.29 ± 0.07

1× (baseline)
54×
81×
26×

0/0
2.0 / 941
60 / 5,969
—

TinyLlama
TinyLlama

5% ± 9%
0.5% ± 1%

0.05 ± 0.09
0.01 ± 0.01

0/0
0.2 / 90

B1b
B3b (ablated)

TinyLlama
TinyLlama

52% ± 35%
3% ± 2%

0.72 ± 0.53
0.03 ± 0.02

1× (baseline)
0.1× (1/4
seeds trig)
10×
0.6×

60 / 8,061
—

Long corpus (180-turn):

Method

Model

Hit rate

Mean specific
keywords

Amplification
(vs Frozen)

n_triggers /
LoRA L2

Frozen
V9

GPT-2
GPT-2

0.5% ± 1%
12.5% ± 8.2%

0.01 ± 0.01
0.13 ± 0.09

1× (baseline)
25×

0/0
4.2 / 1,494

21


--- Page 22 ---
Figure 12: Operational persona-imprint (§4.11): productive-recall hit rate under content-free prompting.
Short corpus: V9 54% ± 44% vs Frozen 1% (54× amplification, high variance). Long corpus: B1b mean
exceeds V9 but with 3× wider variance — V9’s signature is consistency, not higher mean.

Method

Model

Hit rate

Mean specific
keywords

Amplification
(vs Frozen)

n_triggers /
LoRA L2

B1b

GPT-2

24% ± 24.5%

0.29 ± 0.32

180 / 12,996

B3b (ablated)
Frozen
V9
B1b
B3b (ablated)

GPT-2
TinyLlama
TinyLlama
TinyLlama
TinyLlama

26% ± 6%
0% ± 0%
2.5% ± 1.9%
9% ± 5.3%
3% ± 2%

0.29 ± 0.07
0.00
0.03 ± 0.02
0.09 ± 0.05
0.03 ± 0.02

48× (high
variance)
51×
(vs 0)
(vs 0)
(vs 0)
(vs 0)

—
0/0
1.2 / 671
180 / 13,947
—

The headline V9 row is in bold. Figure 12 plots the GPT-2 short and long columns as side-by-side bar charts
with 1σ error bars.
The short-corpus V9-vs-Frozen productive-recall effect is large but underpowered at four seeds (p = 0.095 by
paired t-test); on the long corpus, the key statistical signal is the variance gap against B1b rather than a
higher mean hit rate (§7.4).
Cross-persona discriminating-token test (R2 selectivity): we additionally measure for each generation
whether the model produced this persona’s discriminating tokens (Mandarin/Reykjavik/cello for Alice etc.)
vs other personas’ discriminating tokens (Portuguese/ Marrakech/oboe etc.). Across all V 9 and B1b cells
on both GPT-2 and TinyLlama-Chat, the multi-seed mean discriminating-token count is zero — neither
parametric method generates the specific discriminating tokens at our 40-token generation budget. The only
method that does is B3b RAG on GPT-2 (13.5% target-hit rate vs 2% other-hit rate, a 6.33× selectivity
ratio), because retrieval directly inserts the persona-specific fact into the prompt at evaluation time.
4.11.1

What §4.11 shows that §4.10 does not

1. Productive vs. recognition persona-imprint. §4.10 measures CE under a content-cued prompt;
§4.11 measures the model’s free generation without any content cue. A model that lowers §4.10 CE
but does not amplify §4.11 keyword rate would be one that has “memorised the right continuation”
without internalising any persona structure. On the short corpus both V9 (54% ± 44%, 4 seeds) and
22


--- Page 23 ---
B1b (81% ± 14%) amplify persona-keyword generation by ≥ 54× relative to Frozen (1%), confirming
that the parametric path encodes a generative persona imprint that survives content-free prompting.
B3b (ablated to no-retrieval-context, since the prompt has no fact content for the retriever to
match) sits at 26% on its own — entirely from base-model prior, not from the retrieval pathway.
Compared to §4.10’s recognition-CE metric (where Frozen and V9 differ by less than 1 nats), the
§4.11 productive metric separates Frozen from any parametric-internalisation method by one to two
orders of magnitude in hit rate.
2. V9’s selectivity becomes a consistency property on long streams. Per-seed hit rates on the
long corpus (GPT-2, 4 seeds each) are:
Method

seed 1

seed 2

seed 3

seed 42

mean ± std

V9
B1b

18%
6%

20%
26%

10%
58%

2%
6%

12.5% ± 8.2%
24% ± 24.5%

B1b’s long-corpus hit rate is bimodal — two seeds at noise level (6%) and two seeds where the
every-step LoRA happens to align with persona content (26%, 58%). V9’s hit rate is monotonically
distributed in [2%, 20%]. V9’s selectivity does not raise the mean persona- imprint on
long streams, but it reduces seed-to-seed variance by ∼ 3× (8.2% std vs 24.5% std), while
using 45× fewer LoRA updates (4 events × 5 inner = 20 steps vs B1b’s 180 steps) and 9× smaller
LoRA L2 drift (1,494 vs 12,996). This is the signature of selectivity that survives multi-seed scrutiny:
a parametric path whose persona-imprint is reproducible across stochastic stream orderings, at a
fraction of the parameter budget. (The 2-seed pilot of v0.3 saw B1b = 6% and reported a V 9 > B1b
crossover; the 4-seed batch revealed that the crossover was a low-N artefact and the genuine signature
is the variance gap, not the mean gap. We report both numbers.)
3. Discriminating-token test: parametric encodes typology, retrieval encodes specifics
(R2). Of the ∼ 24 persona-keywords used in §4.11’s “broad” hit-rate metric above, only 3
per persona are truly discriminating (Mandarin/Reykjavik/cello identify Alice; the rest — climbing/Dolomites/bakery/Carnegie/chess/peanuts/etc. — are shared typological features across all five
personas). We separately measure the discriminating-token rate when prompted for persona P : how
often the model emits P ’s discriminating tokens versus how often it emits another persona’s discriminating tokens (cross-persona contamination). The result across all V 9 and B1b cells on both GPT-2
and TinyLlama-Chat: discriminating-token mean and other-persona contamination are both 0. The
parametric path encodes the persona’s typological imprint (climbing/music/languagespeaker themes that show up in §4.11’s broad hit rate) but does not encode the specific
verbatim discriminating tokens at our 40-token generation budget. The only method that
produces discriminating tokens is B3b RAG on GPT-2 (0.135 target / 0.025 other; hit-rate ratio
13.5%/2% = 6.33× selective by retrieval design). This is a functional separation, not a failure mode:
typology is what parametric internalisation is for; verbatim recall is what retrieval is for. §6.3
develops the architectural- complementarity argument.
4. Retrieval cannot produce the typological signature, either. A retrieval system fed an
"About {persona}: {persona} " prompt with no content cue cannot retrieve persona-fact strings
into the context (the prompt is below the retrieval-relevance threshold). The §4.11 typological amplification (V9 54% short, 12.5% long on GPT-2) is by construction the part of persona-consolidation
that retrieval cannot provide, even when its §4.10 CE is the lowest in the table. Symmetrically,
parametric methods cannot provide the discriminating-token specificity that retrieval delivers. The
two roles are complementary and architecturally non-redundant — a hybrid would inherit
both (§4.12 shows naive eval-time concat is insufficient).
5. TinyLlama-Chat under the short corpus does not trigger V9 reliably (1/4 seeds), so the
persona-imprint signature collapses to Frozen. This is consistent with the §4.10 minimum-streamlength finding (V9 fires on 0.2 events/seed mean short, 1.2 events/seed mean long): persona-imprint
23


--- Page 24 ---
requires triggers to have fired. We report this honestly rather than tune τs post-hoc; the §5.2’ P-band
rule and a longer corpus together would resolve it.
4.11.2

A prefix-controlled response-boundary probe

The persona-imprint metric above counts keywords in free generations and is therefore sensitive to prompt
design. We complement it with a mechanistic readout: after training, does a model assign lower cross-entropy
to its own generations than to matched generations sampled from a sibling seed of the same method?
Because a model’s own trained prefix already makes its samples distinctive, we control for working memory
by scoring every model with a single shared reference prefix, so only the LoRA differs across scorers; this
isolates the parametric contribution (the prefix-controlled self-advantage). We also report a magnitude-free
source-attribution accuracy (whether a stimulus’s own model assigns it the lowest CE among the same-method
seeds; chance = 1/4) and the LoRA drift. Frozen has an aggregate prefix-controlled self-advantage of exactly
zero by symmetry (its per-seed values are nonzero but cancel) and its attribution stays at chance — a
structural null (under the shared prefix all Frozen seeds collapse to one model). Stimuli are 6 generations per
persona across 5 personas, 4 seeds, on both streams (w4_runs/ce_probe/).
Across both the 60- and 180-turn streams, V9’s prefix-controlled self-advantage is positive on all four seeds
(+1.87 ± 0.28 short, +1.22 ± 0.89 long) with attribution accuracy 0.87 and 0.94, against the Frozen structural
zero (attribution 0.25). This is consistent with selective parametric writes carving a seed-specific response
boundary. The contrast with naive every-step LoRA (B1b) is sharpest in efficiency and stability: V9 reaches
a comparable or stronger mean boundary at roughly one-sixth to one-ninth the LoRA drift (L2 941 vs 5969
short; 1494 vs 12996 long) and with far lower drift variance. B1b is less stable and much higher-drift — its
boundary is inconsistent on the short stream (+0.29, 1/4 seeds) and emerges only on the long stream (+0.90,
4/4) at ∼ 9× V9’s drift — so the boundary-magnitude gap is corpus-dependent while the drift/stability gap is
robust. The absolute magnitude is likely amplified by partially repetitive generations; we therefore emphasize
sign consistency, attribution accuracy, and the V9-vs-B1b drift contrast rather than the raw nats value alone.

4.12

Corpus

Method

self-adv (prefix-ctrl.)

pos

attribution

LoRA L2

short (60)
short (60)
short (60)
long (180)
long (180)
long (180)

Frozen
V9
B1b
Frozen
V9
B1b

+0.00 ± 0.81
+1.87 ± 0.28
+0.29 ± 0.84
+0.00 ± 1.01
+1.22 ± 0.89
+0.90 ± 1.42

2/4
4/4
1/4
2/4
4/4
4/4

0.25 (chance)
0.87
0.55
0.25 (chance)
0.94
0.87

0
941
5969
0
1494
12996

EVAF ⊕ RAG eval-time hybrid (§6.3 complementarity probe)

To test whether the parametric and retrieval paths are fuseable in practice — not merely complementary
in theory — we implement a minimal eval-time hybrid: the stream is processed by V9 (dual-gate LoRA
consolidation, identical to §4.10 V9), but at evaluation time each prompt is prefixed with the top-K = 4
Sentence-BERT-retrieved turns from the same stream (identical retrieval to B3b). No additional parameters
are trained; this is a lower bound on what a full write-time routing hybrid could achieve.
GPT-2, 4 seeds (full table in w4_runs/aggregates/persona_gen_table.md):
Metric

V9

B3b RAG

§4.10 recognition CE,
short
§4.10 recognition CE,
long

5.75 ± 0.58

2.18

7.06 ± 0.96

2.18

24

Hybrid (V9 stream +
RAG eval)
5.49 ± 0.68 (ns vs V9,
p = 0.45)
7.67 ± 0.82 (ns vs V9)


--- Page 25 ---
Metric

V9

B3b RAG

Hybrid (V9 stream +
RAG eval)

§4.11 broad hit rate,
short
§4.11 broad hit rate,
long
§4.11 discriminating
target hit

54% ± 44%

26% ± 6%

53% ± 45% (≈ V9)

12.5% ± 8.2%

25.5% ± 6%

12.0% ± 8.2% (≈ V9)

0%

13.5%

0.5% (≈ V9, not B3b)

What the hybrid shows:
1. Recognition: naive eval-time concatenation does not close the gap to B3b (p < 0.01 Hybrid vs
B3b on CE). The hybrid is statistically indistinguishable from V9 alone on both corpora — RAG
context at eval does not materially improve the recognition metric when V9’s LoRA+prefix is already
present.
2. Productive typology: hybrid broad hit rate tracks V9 within 1σ on both corpora (53% vs 54%
short) — the parametric persona imprint dominates free generation even with retrieved context
prepended.
3. Productive verbatim: hybrid discriminating-token rate remains ≈ 0%, far below B3b’s 13.5%.
Retrieved facts in the prompt do not surface as discriminating tokens in the generated continuation —
the LoRA-shaped generative distribution overrides the retrieval prefix at our 40-token budget.
Interpretation: Hybrid inherits V9’s typology channel within 1σ on both corpora, while its discriminatingtoken rate stays 0.0–0.5% — far below B3b’s 13.5%. Thus eval-time concatenation confirms component-level
orthogonality but is not sufficient fusion; a production hybrid would need write-time routing and likely
generation-time gating. We scope full write-time routing hybrid as the primary follow-up in §6.3.

5

Scaling Rules and Their Regime of Validity

We now formalise why the hyperparameters (τs , λreg , lrLoRA ) must be re-anchored when the base model
changes, and to what extent the re-anchoring rules are predictive rather than calibrated. The discussion
proceeds in four blocks: a geometric-lock δ bound (§5.1), an adaptive percentile τs rule (§5.2), an EWC
anchor λreg multi-factor Langevin form (§5.3), and a 4-point empirical audit that delimits each rule’s regime
of validity (§5.4). Where a rule fails on a new model, we report it.
5.1

Geometric lockdown bound on δ := ∥s∥2 −

√

PD

The spherical projection in §3.1 enforces ∥s∥2 = P D at every step. The empirically observed deviation δt
comes from two regimes:
δt ≤ max


√cts μ∇ αstep
PD

|

{z

,

}

(A) thin-shell tangential residual

√
Crnd εmach P D
|
{z
}


(5.1)

(B) FP32 random-walk round-off

The first term originates from the discrete-time Euler integration of the √
spherical gradient flow: the tangential
√
component of the unconstrained step has typical magnitude μ∇ αstep / D, and after T steps a O(1/ D)
residual along the radial direction accumulates. The second term is the floating-point round-off contribution
from T summation operations, each of relative precision εmach = 2−23 in FP32.
Empirical validation across 4 models:
25


--- Page 26 ---
Model

D

predicted (B)

measured max δ

GPT-2 124M
TinyLlama-Chat 1.1B
Qwen3-Base 1.7B
Mistral-Base 7B

768
2048
2048
4096

∼ 4 · 10
∼ 1.4 · 10−5
∼ 1.4 · 10−5
∼ 2.4 · 10−5

4.34 · 10−6
1.32 · 10−5
1.32 · 10−5
1.28 · 10−5

−6

All four points lie within the FP32-round-off
branch of (5.1); the thin-shell branch (A) is sub-dominant at
√
the αstep values used. The O(1/ D) dimension-scaling is consistent with the prediction up to a factor of ∼ 2
that absorbs constants cts , Crnd . No data point falsifies (5.1).
5.2

Adaptive percentile τs — the P-band rule

The dual gate Pwrite = σ(kv (V − τv )) σ(k(S − τs )) is sensitive to where τs sits relative to the realised
distribution of S(t). A naively transferred fixed τs = 6.0 from GPT-2 to TinyLlama-Chat (μS = 12.3) leaves
the surprise gate near-permanently open, degrading the dual gate to a valence-only gate; in §4.8 we showed
this produces V8-style catastrophic forgetting.
Our original single-factor prescription was the median rule:

τs := Pct50 S(t) : t < Twarmup ,

(5.2)

which (i) is model-invariant by construction, (ii) lands the gate near the densest part of the S(t) distribution,
retaining ∼ 50% surprise-side selectivity for the joint gate, and (iii) requires no hyperparameter search. In
the 4-point cross-model audit we validated (5.2) on Qwen3-Base, where the rule worked first-try (μS = 5.37,
τs = 4.1 ≈ Pct50 (Swarmup )); but on Mistral-7B-Base it produced near-runaway gate firing (17 triggers,
∆S global = +12.6). The empirical fix that worked was to raise τs to ∼ Pct95 (Swarmup ) = 5.0, reducing the
trigger count to 5 and restoring healthy negative ∆S (Table 5).
Inspecting the warmup distributions reveals why: Mistral-Base’s IQR(Swarmup ) = 0.5, half of Qwen3-Base
(1.0) and TinyLlama-Chat (2.1). A narrow S distribution means Pct50 sits among the most common samples,
not the most surprising ones, so a 50%-quantile gate admits half the stream. The post-audit revision is the
P-band rule:

τs :=

(
Pct50 (Swarmup )
Pct75 (Swarmup ) to Pct90 (Swarmup )

if IQR(Swarmup ) ≥ 0.8 (Chat / wide-S Base)
if IQR(Swarmup ) < 0.8 (deep-pretrained Base)

(5.2’)

(5.2’) is a post-hoc refinement: we treat it as a working rule motivated by the 4-point cross-model data, not a
closed-form theoretical prediction.
5.3

EWC anchor strength λreg — multi-factor Langevin form

The L2 anchor term λreg ·∥θLoRA −θ0 ∥2 in §3.2 protects against catastrophic forgetting. Following a mean-field
overdamped Langevin equilibrium argument (Math Framework §11.3 EQ. 11.4), the steady-state λreg should
satisfy

σ̄g2 μS (M )
λreg (M ) ∝
.
target per-param ∥θ − θ0 ∥2 / nLoRA (M )

(5.3)

The numerator is the gradient-noise variance (proxied by μ2S ); the denominator is the target per-parameter
equilibrium displacement, which expresses how far we are willing to let the LoRA weights move from their
26


--- Page 27 ---
initialisation per parameter. (5.3) gives a two-factor prediction: how much λreg should grow with μ2S and
how it should shrink as per-param drift budget grows.
Empirical 4-point audit (compared to GPT-2’s λreg = 10−3 , μS = 7.3, nLoRA = 4.72 M):
Model

μ2S ratio

nLoRA ratio

observed
λreg /λGPT-2

implied per-param
L2 ratio

TinyLlama-Chat
1.1B

2.84

1.91

10.0

Qwen3-Base 1.7B

0.54

2.72

0.5

Mistral-Base 7B

0.26

5.78

50.0

3.6 (Chat
compresses
harder)
≈ 1.0
(single-factor μ2S
rule suffices)
≈ 33 (super-linear
capacity coupling)

The Qwen3 point is consistent with the single-variable simplification λreg ∝ μ2S — the implied per-param
L2 ratio is ∼ 1. The TinyLlama point requires a Chat-vs-Base correction (Chat models compress to a tighter
equilibrium). The Mistral point requires a super-linear capacity coupling not present in (5.3): the implied
per-param L2 ratio of 33 is inconsistent with simple Langevin equilibrium and suggests that at ≳ 20 M LoRA
parameters, gradient cross-correlations across LoRA blocks make individual per-parameter drift sub-additive.
We therefore present (5.3) as the interpretive framework for the empirical λreg values rather than a
closed-form a-priori predictor. The 4-point fit suggests an empirical interpolation

log10 λreg (M )


≈ log10 λGPT-2 + 2 log10 μS /μS,GPT-2 + β(M ) · log10 nLoRA /nLoRA, GPT-2 ,

(5.3’)

with β(Chat) ≈ 1, β(Base, ≤ 2 B) ≈ 0, β(Base, ≥ 7 B) ≈ 2. The Base ≤ 2 B and Base ≥ 7 B regimes are each
supported by one data point (Qwen3, Mistral), so β should be regarded as fitted, not predicted; we list
this as a paper limitation (§7). A direct measurement of Fisher trace, in lieu of the uniform-Fisher proxy
used here, would in principle replace (5.3’) with a one-factor rule and is the most natural follow-up.
5.4

New conjecture: lrLoRA scale-aware (one-point support)

The cross-model audit surfaced a hyperparameter axis that the original single-factor rule did not consider:
lrLoRA itself must shrink for deep-basin Base models. On Mistral-Base, the GPT-2 / TinyLlama / Qwen3-Base
setting lrLoRA = 5×10−3 produced runaway drift; only after lowering it to 5×10−4 did the four V9 signatures
emerge. We conjecture
lrLoRA (M ) ∝ p

1
1

 ,
·
nLoRA (M ) 1 + cmaturity μS,ref /μS (M ) − 1 +

(5.4)

with μS,ref ≈ 7 matching the GPT-2 baseline. The first factor is the standard SGD step-size scaling for stable
optimisation with nLoRA parameters; the second factor expresses that as the base model’s intrinsic surprise
drops (it becomes more deeply pretrained), the deterministic gradient component becomes large relative to
noise and the effective step must shrink to preserve selectivity. This is one-point supported (Mistral-Base
7B). The natural validation would be Llama-3-8B Base or Qwen3-7B Base, both within budget.
5.5

Falsifiability summary and what the cross-model audit changed

27


--- Page 28 ---
Rule

Form

Validation status at 4 points

§5.1 geometric lock δ
§5.2 τs percentile

EQ (5.1)
EQ (5.2) → EQ (5.2’)

§5.3 λreg multi-factor

EQ (5.3)

§5.4 lrLoRA scale-aware

EQ (5.4)

confirmed; 4/4 within bound
revised post-Mistral; P-band
rule by IQR(Swarmup )
form consistent at 4 points but
closed-form prediction requires
Fisher-trace measurement; (5.3’)
is an interpolation, not a
prediction
new conjecture, single-point
empirical support

Calling out the failures of the single-variable simplifications is, we believe, a feature of the framework: each
non-trivial test result on Qwen3 and Mistral pinpoints which axis of the scaling rule needs revision, and the
revisions themselves are formally well-posed. Section 7 lists the additional data points needed to convert
(5.3’) and (5.4) from interpolations and conjectures into closed-form predictors.

6

Discussion

6.1

Two-component physics: spherical lockdown × selective LoRA gating

EVAF’s mechanistic behaviour decomposes into two components that operate at different time-scales and
on different substrates. The fast component is the spherical-attractor dynamics of §3.1: a workingmemory√state s(t) descending
√ the surprise-gradient field on the high-dimensional per-token product manifold
(S D−1 ( D))P ⊆ S P D−1 ( P D). The hard sphere constraint is faithful√because of thin-shell concentration
(§5.1): in RP D with D ≳ 103 random initialisations live within O(1/ P D) of the sphere naturally. The
constraint therefore prevents only an infinitesimal deformation while preventing the finite runaway-norm
pathology that unconstrained recurrence is known to suffer.
The slow component is the LoRA consolidation pathway of §3.2: parameter updates fire only when the dual
gate Pwrite (V, S) > 0.55 admits ≥ 4 samples to the buffer, and each event is regularised by an EWC- style L2
anchor on the LoRA weights. The two components couple in one direction only: the prefix state s(t) is held
fixed during the LoRA inner loop, while the LoRA delta participates in the prefix dynamics in subsequent
steps. This one-way coupling is what makes V9 trainable in practice; the alternative (joint optimisation of s
and θ) risks a fast/slow timescale mismatch leading to either runaway prefix drift or LoRA divergence.
What the four §1 mechanistic signatures together imply about the physics:
• (i) ∆S B < 0 on every event, ∆S R tracking ∆S B : the LoRA inner loop is in fact learning, and the
write is retained on replayed (trained) past samples — not rote-fitting (§7.5 audits the rote-fit
alternative via the C1 random- token replay control; the held-out ∆S H (θ) probe of §4.2 adds
the genuine unseen-data check).
• (ii) Post-event step-angle freeze (θpost ≪ θpre ): once the LoRA delta brings the model into near-zero
surprise on the consolidated samples, the gradient field itself loses its slope along s, so the
working-memory trajectory naturally relaxes into a fixed point.
√
• (iii) Geometric lockdown bound δ ≤ Crnd εmach P D: the residual deviation from the sphere is at
the FP32 round-off floor, not a physical effect.
• (iv) Capacity-and-maturity-aware λreg : the EWC anchor is necessary (without it, post-event drift
escapes the regulariser basin within a few events; cf. C2 ablation and the B2 catastrophic
forgetting at TinyLlama scale).
28


--- Page 29 ---
These four signatures hold across ×57 parameter scale and four distinct attention architectures, which we
interpret as evidence that the spherical-attractor + dual-gate-LoRA physics is substrate-invariant within
the LLM family — not a property of any specific architecture’s optimiser or initialisation.
6.2

Cognitive-science connection: an architectural pattern, not a claim about brains

The Complementary Learning Systems hypothesis (McClelland et al., 1995) posits two memory stores with
a selective transfer gate that consolidates hippocampal episodes into neocortical weights over time. EVAF
instantiates the same architectural pattern: a fast prefix-state store with rapid inference-time relaxation,
a slow LoRA-weights store updated under a selective gate. We make this connection explicit because it
motivated the original design and is, we believe, a useful framing for readers from cognitive science. We do
not claim that the spherical attractor maps to hippocampal CA3-CA1 dynamics, that the dual gate maps
to a specific neuromodulatory signal, or that the EWC anchor reflects a biological consolidation timescale.
The reasons for this restraint are honest: our experiments use synthetic controlled corpora and a heuristic
/ DistilBERT-SST2 valence oracle. None of these admit comparison to neural data without an additional
experimental layer (paired LLM-brain decoding, in-vivo fMRI consolidation signatures, etc.) that lies outside
the scope of the present paper. The CLS connection is therefore suggestive at the architectural level and we
flag it as such in the abstract and §1.
6.3

EVAF and retrieval operate on different timescales for different purposes

The persona case study (§4.10–§4.11) makes the trade-off concrete and, more importantly, separates the two
memory operations that the LLM-memory literature has sometimes conflated.
Retrieval is a query-time fact-lookup operation. A retrieval-augmented system stores content in
an external index and returns the most relevant items at inference time. The parameters of the LLM are
unchanged by the act of storing or retrieving. On the §4.10 recognition metric Sentence- BERT MiniLM RAG
with top-K = 4 retrieval produces a CE of 2.18 on GPT-2, dominating every parametric method including
V9 (5.75). This is by design: a verbatim-recall metric is the natural strength of retrieval, since the retriever
just returns the stored text. The 2024-2026 SOTA retrieval-memory systems — Mem0, Zep, Letta/MemGPT
(Packer et al., 2023), HippoRAG (Gutiérrez et al., 2024), LIGHT (Tavakoli et al., 2025), EmergenceMem,
Mastra OM — achieve 90–99% on LongMemEval (Wu et al., 2024) by perfecting this operation, and we
explicitly do not propose to replace them.
EVAF is a long-timescale persona-consolidation operation. The parametric path that EVAF
instantiates does something that retrieval cannot do by construction: it changes the model itself in response
to selectively-filtered stream content. The signature of this change is the §4.11 productive-recall amplification
— an EVAF-trained model freely generates persona-keyword content at 4-seed mean 54× the frozen- base rate
(54% ± 44% V9 vs 1% frozen on the short corpus, GPT-2) on a prompt that contains no fact content that
could be retrieved. Retrieval cannot produce this signature even in principle, because the prompt is below
the retrieval-relevance threshold; the persona imprint lives in the model’s parameters or it does not exist at
all. The §4.10 recognition CE does not see this signature because it cues the prompt with persona content
that the retriever can match; the §4.11 productive-recall metric isolates it.
R2 reveals that the parametric / retrieval split is functional, not merely a leaderboard fact.
§4.11’s discriminating-token test (R2) sharpens this: of the persona-keywords driving the broad amplification,
some are typological (climbing/music/language-speaker — shared themes that show up identically across all
five personas) and others are discriminating (Mandarin/Reykjavik/cello uniquely identify Alice). Across every
V 9 and B1b cell on both GPT-2 and TinyLlama-Chat, the parametric methods emit zero discriminating
tokens; the only method that emits discriminating tokens is B3b RAG (6.3× selective by hit-ratio on
GPT-2). And symmetrically, B3b’s typological-broad hit rate is 26%, well below V9’s 54% on the short
corpus, because the broad-hit metric is content-free at the prompt level and retrieval cannot help. The R2
discriminating-token test is our strongest direct evidence for complementarity: in one controlled
design it simultaneously falsifies parametric verbatim recall (0% discriminating tokens across all V9/B1b cells)
29


--- Page 30 ---
and retrieval typological amplification (B3b broad hit 26% vs V9 54% on the short corpus). The parametric
and retrieval paths therefore live on orthogonal axes of the persona-imprint signature:
Typological imprint (broad
keyword amp)
Parametric (V9, B1b)
Retrieval (B3b RAG)
Eval-time hybrid (§4.12)

✓ (54–81% hit rate on short,
12–24% on long)
✗ (26%, no better than B3b
ablated baseline)
✓ (53%, tracks V9)

Verbatim discriminating tokens
✗ (0% at our 40-token budget)
✓ (6.3× selective by hit-ratio)
✗ (0.5%, tracks V9 not B3b)

This is the clean separation between “the agent that does the retrieving” (parametric: identity/typology, slow
timescale) and “the facts the agent retrieves” (retrieval: verbatim content, query timescale). The two are not
competitors on a leaderboard but architecturally non-redundant memory primitives.
§4.12 eval-time hybrid probe. V9 stream consolidation plus top-K RAG at eval confirms component-level
orthogonality but rejects naive concatenation as fusion: Hybrid recognition CE is statistically indistinguishable
from V9 alone (p = 0.45 short corpus), and discriminating-token rate remains ≈ 0% vs B3b’s 13.5%. A
production hybrid therefore needs write-time routing and likely generation-time gating, not eval-time
string concatenation.
This is a qualitative timescale distinction, not just a metric choice: retrieval operates on the query timescale
(one user turn at a time; the store grows monotonically and the parameters are static); EVAF operates on
the persona-formation timescale (the cumulative selectivity of hundreds-to-thousands of dual-gate decisions
slowly sculpts the parameters into a model that thinks and writes like the agent it has become). In CLS
terms (McClelland et al., 1995; Kumaran et al., 2016), retrieval implements the hippocampal-store analogue
without a slow store, while EVAF implements the slow-store analogue without an external index. The two
are architecturally complementary, and we believe a long-horizon agent system that cumulatively becomes
someone should ultimately combine them via write-time routing (§4.12), not naive eval-time concatenation.
6.4

The scaling-rules story: predictive vs. descriptive

§5 presents three scaling rules. The 4-point cross-model audit gives an honest report card:
• §5.1 (δ-bound): predictive. 4/4 points within bound at the predicted dimension scaling.
• §5.2 (τs ): descriptive at single-rule, predictive at P-band. The original Pct50 form works
on Chat / wide-S Base models (GPT-2 ✓, TinyLlama-Chat ✓, Qwen3-Base ✓); fails on narrow-S
deep-pretrained Base models (Mistral-Base requires Pct75−90 ). The post-hoc P-band rule (5.2’)
correctly accounts for the 4-model spread by gating on IQR(Swarmup ).
• §5.3 (λreg ): descriptive at multi-factor, only predictive within a model class. The singlevariable μ2S rule works for Qwen3-Base but requires Chat-vs-Base correction for TinyLlama and
capacity-superlinear correction for Mistral. The empirical interpolation (5.3’) has three free parameters
fitted on ≤ 4 points; we list this as a paper limitation (§7.2).
• §5.4 (lrLoRA ): conjecture, one-point support. Mistral-Base only. Llama-3-8B-Base or Qwen3-7BBase would falsify or confirm the maturity-aware form.
What we believe this means: the form of EVAF’s scaling rules is on a solid footing — thin-shell concentration
for §5.1, EWC norm consistency for §5.3, percentile selectivity for §5.2 — but the predictive power of the
closed-form versions is currently bounded by the size of the audit (four models). A natural revision cycle
adds a 5th-7th model to discriminate the §5.3’ and §5.4 candidate functional forms.
6.5

What we believe is novel

Scope. EVAF should be interpreted as a falsifiable protocol for studying selective parametric
consolidation, not a complete theory of identity formation. We retain persona consolidation as the
30


--- Page 31 ---
motivating research question — which experiences should shape parameters rather than an external index —
while claiming only measurable signatures consistent with that process (§4.10–§4.12), not a proof of
cognitive identity formation. The persona-fact experiments (§4.10–§4.12) are a downstream application
case study, not the paper’s primary claim: the title-level contribution is the parametric-consolidation
protocol and its mechanistic signatures (§3–§4.9), for which persona is one illustrative use case.
To be explicit, our specific contributions are (cf. the §2.6 table):
• The dual gate Pwrite = σ(kv (V − τv )) σ(k(S − τs )) with a V × S product form (multi-component
selectivity is required in §4.4 ablation), framed as a persona-relevance filter rather than a generic
surprise filter.
• The Test-Retest protocol as a falsifiable harness for detecting parametric writes with replayretention (anti-forgetting) checks in continual-learning systems, complementing the existing localisation / edit-propagation literature on memory mechanism in LLMs.
• The persona-imprint signature — an operational productive-recall metric under contentfree prompting (§4.11) — as a generative-level probe consistent with long-timescale selective
consolidation that recognition-CE metrics cannot detect and retrieval-augmented systems cannot
produce by construction.
• The operational dynamical-freeze z-score as a clean discriminator between counterfactual
ablations of the consolidation event — it separates V9 from every C0–C4 ablation on every model
(not a formal phase-transition proof; §7.15).
• The multi-factor λreg scaling rule (§5.3) and the honest regime-of-validity audit it received in
the cross-model audit (Qwen3 fits the single-factor form; Mistral exposes the need for a maturity-aware
correction and an lrLoRA co-revision).
We borrow LoRA (Hu et al., 2022), experience replay (Rolnick et al., 2019), EWC (Kirkpatrick et al.,
2017), the thin-shell concentration limit (Vershynin, 2018), and the CLS architectural pattern (McClelland
et al., 1995); the combination, the spherical-attractor formulation, the persona- consolidation framing with
productive-recall verification, and the mechanistic protocols are the new content.
6.6

What remains open
• §6.4 enumerates the missing data points needed to upgrade the §5.3’ / §5.4 interpolations to predictive
forms.
• §4.10 raises the natural follow-up question of whether the selectivity-and-stability framing of V9
transfers to non-verbatim- recall downstream tasks (LongMemEval, LoCoMo, MemoryArena); we list
these under §7.7.
• §4.12 implemented the eval-time hybrid probe (V9 stream + RAG at eval, GPT-2, 4 seeds), confirming
orthogonality but rejecting naive concatenation as fusion. Write-time routing — high V × S →
consolidation; verbatim facts → index — remains the primary follow-up for a production hybrid.
• The CLS architectural connection (§6.2) could be tested at the neuroscience interface; this is a
separate paper and we do not attempt it here.

7

Limitations and Open Questions

We list limitations in order from most likely to constrain the paper’s scope to most peripheral.
7.1

Controlled corpus and synthetic eval

The four mechanistic experiments and the persona-fact retention task (§4.10) all run on controlled, designed
inputs: the 210-sentence base corpus for §4.1-§4.9 covers ten semantic categories of mixed valence and surprise;
the persona stream interleaves 30 persona-facts with either 30 (short) or 150 (long) generic-knowledge fillers.
We have not validated EVAF on open-domain agent conversations or on naturally-occurring noisy text. The
principal risk this exposes is that real conversational distributions may have a fatter S(t) tail than our
31


--- Page 32 ---
controlled corpus, which would push the gate firing rate up; whether the multi-factor λreg rule (§5.3) remains
valid in that regime is open.
7.2

Scaling rules: form vs. predictive power

After the 4-point cross-model audit, each §5 rule falls into one of three epistemic classes (full discussion in
§6.4):
Rule

Class

What we can claim

§5.1 (δ-bound)

Predictive

§5.2 (τs / P-band)

Partially predictive

§5.3 (λreg )

Descriptive / fitted

§5.4 (lrLoRA )

Conjecture

4/4 models within the
thin-shell-derived bound at
predicted dimension scaling
Pct50 fits Chat/wide-S Base
models; P-band (5.2’) accounts
for all 4 models — but IQR
threshold (0.8) is fitted, not
derived
Single-factor μ2S predicted
Qwen3-Base only; Chat (×3.6)
and capacity (×33) corrections
are post-hoc; interpolation (5.3’)
has 3 free parameters on ≤ 4
points — not yet predictive for a
5th model
One-point support (Mistral-Base
only)

The form of each rule (thin-shell for §5.1, percentile selectivity for §5.2, EWC norm consistency for §5.3) is
on solid footing; the limitation is predictive power bounded by audit size, not theoretical derivation. A
5th–7th model (Llama-3-8B-Base or Qwen3-7B-Base) would discriminate the §5.3’ and §5.4 candidate forms
and constrain the capacity exponent β(M ) in (5.3’).
7.3

Multi-seed coverage is uneven

Coverage at submission is: GPT-2 V9 / B1a / B2 at 5 seeds, GPT-2 B1b / B3a / B3b at 1 seed, TinyLlama
V9 / B1a / B2 at 4 seeds, TinyLlama B1b / B3a / B3b at 1 seed, Qwen3-Base and Mistral-Base at 1
seed each. The headline V9-vs-baselines claims of §4.9 are reported at multi-seed (§4.9 multi-seed report);
the cross-model scaling claims of §4.8 and §5 are reported at single-seed, with the sign of every signature
consistent. A reviewer-anticipated request for multi-seed Qwen3 and Mistral is a 2-day follow-up; we list it
under “future work, paper revision”.
7.4

Statistical inference (updated v0.7)

Pre-specified vs exploratory comparisons. Headline multi-seed comparisons in §4.10–§4.11 (V9 vs Frozen,
V9 vs B1b, B3b vs parametric methods on recognition CE; productive-recall hit rate and discriminating-token
rates) were pre-specified before aggregation. Ablation-matrix panels (§4.4), oracle-swap runs (§4.6), scalingrule audits (§5), and second-order findings (§1 C6) are descriptive / exploratory; we do not apply
multiple-comparison correction across the full matrix and report them as mechanism-characterisation, not as
independent hypothesis tests.
We prioritise effect sizes and cross-seed robustness (mean ± std, variance ratios) over p-values
where n = 4 seeds limits power. Full paired Wilcoxon signed- rank and paired t-tests are in
w4_runs/aggregates/w4_stat_tests.md.
32


--- Page 33 ---
Headline results (GPT-2): V9 vs Frozen recognition CE on the 60-turn stream is highly significant by paired
t-test (p < 0.001); V9 vs B1b on the same cell is borderline (p = 0.058). B3b vs every parametric method is
significant (p < 0.01) on recognition CE — expected for a verbatim- retrieval metric. On productive-recall hit
rate, B3b vs Frozen is significant (p < 0.01 short corpus) but V9 vs Frozen does not reach p < 0.05 on either
test (t-test p = 0.095 short), reflecting the high seed-to-seed variance intrinsic to V9’s few-write selectivity
(§7.12). We do not over-interpret non-significant Wilcoxon results at n = 4; the variance-ratio analysis in
w4_stat_tests.md (V9 std 2.98× lower than B1b on GPT-2 long corpus hit rate) provides complementary
evidence for the consistency claim.
7.5

Test-Retest detects parametric writes and replay-retention, not held-out generalization

∆Sbuf measures the change in surprise on the buffer samples after the LoRA inner loop. A within-event drop
is consistent with a parametric write on the admitted material. ∆Srep measures the change on the
replay set R — but R is part of the same inner-loop training batch (B ∪ R, EQ 3.3), so a negative ∆Srep is
a replay-retention (anti-forgetting) check on trained past samples, not a held-out generalization
measure. A counterfactual that nulls the LoRA inner loop produces ∆S = +0.000 (C0/C3), confirming the
drop is optimisation-induced; but this does not rule out a “rote-fit” reading. Indeed §7.6 shows that even
random-token replay (C1) yields a sizeable negative ∆Srep , so ∆Srep < 0 alone is weak evidence — we rely
on the step-angle freeze (§4.3) as the primary discriminator. We therefore added a held-out probe ∆S H (θ)
on a set H that never enters any inner loop (§4.2). On GPT-2 only the full system lowers held-out surprise
(−16%) while every still-trained ablation raises it — evidence that the write generalizes rather than rote-fits —
whereas on TinyLlama-Chat the parameter-only effect is near-neutral (we observe only an event-local rescue
that is confounded by working-memory prefix drift, §4.2). The deeper held-out and response-boundary probes
(§4.2, §4.11) are thus reported in the GPT-2 calibrated regime; the cross-model experiments support the core
write/freeze signatures, while final param-only held-out transfer remains model-dependent. We do not claim
cognitive identity formation from these surprise measurements.
7.6

Noise-fitting trap

A subtle failure mode revealed by ablation C1 (random-token replay): even nonsensical replay content
produces a sizeable ∆Srep , because the optimiser can fit any distribution given enough capacity. This sets a
lower bound on what ∆Srep alone can tell us — its magnitude is sensitive to noise budget, not just to genuine
generalisation. The §4.4 ablation matrix is designed so that C1 intentionally falsifies the simplistic reading of
∆Srep ; we use the post-event step-angle freeze (∆θpost ≪ 17.7◦ ) as the secondary discriminator, since C1 fails
it (∆θpost = 20.37◦ , exceeding V5 baseline).
7.7

Persona consolidation is tested on a single, designed task family

The persona case study (§4.10–§4.11) demonstrates persona-consolidation transfer on a single task family
(persona-fact recognition CE + persona-keyword productive recall). The §4.10 recognition metric directly
favours retrieval baselines (B3b) that store and retrieve exact text; the §4.11 productive recall metric is
content-cued by persona name only and isolates the parametric imprint that retrieval cannot produce, but it
is a designed keyword-counting metric, not an external benchmark. We have not tested EVAF on (a) the
multi-session contradiction / update sub-tasks of LongMemEval (Wu et al., 2024), (b) the multi-hop reasoning
sub-tasks of LoCoMo (Maharana et al., 2024), (c) the inter- dependent agentic tasks of MemoryArena, or
(d) free-form persona-consistent dialogue generation. The recognition / productive-recall pair we report is
internally consistent and the simplest setting in which the persona-consolidation signature is unambiguous;
extending to external benchmarks is scoped for the revision and is one of the most decision-relevant open
items. We additionally report a prefix-controlled response-boundary probe (§4.11): sampling generations
from each trained seed and scoring them under a shared reference prefix so only the LoRA differs, V9 shows a
seed-specific parametric boundary on all four seeds at six-to-nine-fold lower LoRA drift than naive every-step
updating (B1b), consistent with selective consolidation carving a response boundary beyond keyword-count
metrics. Its absolute magnitude is likely amplified by partially repetitive generations, so we emphasise sign
consistency, attribution accuracy, and the drift contrast rather than the raw nats value.
33


--- Page 34 ---
7.8

RAG comparison is in-memory; production retrieval will be slower

The Sentence-BERT RAG baseline (B3b) we report assumes in-memory cosine retrieval with the 30- or
180-element store of our experiments; we measure 0.68 ms / query on this scale. Production RAG against a
vector database with 106 + entries would add network and ANN-index latency on the order of 10 ms / query.
Our zero-latency claim for V9 is robust at all scales; the relative latency advantage of V9 over RAG grows
with corpus size, but this extrapolation is not directly measured.
7.9

CLS framing is inspirational, not empirically grounded

We position EVAF against the complementary-learning-systems literature in §1 and §2.1 as architectural
motivation. We do not provide neuroscientific evidence that our LoRA edits correspond to neocortical
consolidation, nor that the spherical attractor maps to hippocampal CA3-CA1 dynamics. The CLS connection
is at the level of “two stores with a selective write gate”, and a careful neuroscience-side test (e.g., comparing
∆S B dynamics to fMRI consolidation signatures) is a separate paper. The manuscript states this explicitly in
§6.2.
7.10

Compute-equivalent baselines: a residual concern

The compute budget of B1a is matched to V9 by construction (same number of triggers and inner steps).
The compute budget of B1b is higher (every-step LoRA update), B3a and B3b are parameter-free, and B2
is variable. We have not normalised wall-clock or FLOPs across baselines explicitly. A compute-controlled
rerun is scoped for the revision.
7.11

End-of-stream evaluation drift (negative result)

We attempted a stress-test of the “selectivity gives V9 a long-stream advantage” hypothesis by extending the
persona-fact corpus to 600 turns (Figure not shown; see supplementary smoke-test artefacts). The result was
a negative finding: V9 and B1b both worsen in end-of-stream persona-fact CE as the stream grows, and their
gap does not widen in V9’s favour. On GPT-2 seed=42 across 60/180/600 turns:
Stream

V9 CE

V9 LoRA L2

B1b CE

B1b LoRA L2

60
180
600

5.66
7.83
8.47

1,031
1,197
1,536

6.46
7.39
8.02

5,724
10,086
∼ 30,000 (extrapolated)

We interpret this as a faithful expression of end-of-stream prefix drift: after ≫ 100 turns, the working-memory
prefix has moved sufficiently far from its state at the persona-fact processing moment that even a perfectlyinternalised LoRA weight cannot recover the fact at the eval-time prefix. This is not a method-specific
failure of V9 vs. B1b; it is a universal eval-protocol issue. The genuine V9 advantage that survives
the long- stream regime is a 20× smaller LoRA parameter drift (1536 vs ∼ 30,000) and a 150× smaller
optimisation-step count (4 events × 5 inner steps vs 600 every-step updates), at comparable end-of-stream
persona-fact CE. The cleanest follow-up evaluations that would expose V9’s selectivity advantage are: (a)
eval at each training-time prefix state (per-event evaluation), not the end-of-stream state — closer in spirit
to the Test-Retest measurement of §3.4; (b) an adversarial-filler stream where high-S low-V decoys should be
filtered by V9’s dual gate but admitted by B1b — direct test of selectivity; or (c) a multi-session contradiction
stream where the user updates a stated fact, testing whether V9’s selective gate handles updates more cleanly
than B1b’s eager updating. We list (a)-(c) as concrete follow-up experiments for the revision.
7.12

Persona-imprint amplification is seed-variable on short streams

The §4.11 productive-recall amplification is seed-variable on the short corpus because V9 fires only ∼ 2 events
on a 60-turn stream; the persona content of those 2 events determines the keyword distribution of post-stream
34


--- Page 35 ---
free generations. The multi-seed distribution we report is GPT-2 V9 54% ± 44% across 4 seeds (per-seed hit
rates {8, 26, 98, 84}% — all strictly above the Frozen 1% baseline, but with a > 12× range across the four
seeds). On the 180-turn corpus V9’s mean is 12.5% ± 8.2% (per-seed {18, 20, 10, 2}%); B1b is 24% ± 24.5%
(per-seed {6, 26, 58, 6}%, bimodal). We treat this as a feature of selectivity — V9 deliberately commits a
small number of high-value writes per stream, so individual writes have outsized influence on the generative
distribution — not a flaw of measurement. The honest reading of the 4-seed data is that V9’s signature
on long streams is variance reduction (3× tighter than B1b) at lower mean, not higher mean: V9 trades
peak performance for reproducibility, at 45× fewer LoRA updates and 9× smaller LoRA L2 drift. (An earlier
2-seed pilot reported a stream- length crossover V 9 > B1b on long; the 4-seed batch revealed that was a
low-N artefact and the genuine signature is the variance gap.) TinyLlama-Chat short-corpus runs do not
trigger V9 reliably (1/4 seeds, consistent with the §4.10 minimum-stream-length finding) and we report this
transparently rather than tune τs to force triggers.
7.13

Parametric methods do not produce verbatim discriminating tokens (R2)

The §4.11 R2 discriminating-token test (Mandarin/Reykjavik/cello for Alice etc.) reveals that neither V9
nor B1b emits the truly-discriminating tokens at our 40-token / T = 0.8 generation budget on either GPT-2
or TinyLlama-Chat (0/50 hit on every cell, 4 seeds). The parametric path encodes the persona’s typology
(climbing/music/language-speaker themes that drive the §4.11 broad amplification) but not the verbatim
discriminating specifics. We frame this as a feature, not a limitation: it is exactly the architectural separation
§6.3 argues for (typology → parametric, verbatim → retrieval). But two genuine caveats follow:
(i) The R2 test as defined cannot detect a V9 vs. B1b selectivity advantage on discriminating tokens
because the floor is 0 for both. A version of R2 with
(a) longer generations (≥ 100 tokens), (b) a larger or chat-tuned base model (e.g. Mistral-7B-Instruct), or
(c) a cued prompt that gives the model permission to list facts (e.g. “List 5 facts about {persona}:”)
would likely surface a non-zero discriminating-token rate and let R2 differentiate parametric methods
on their specific-token imprint, not just their typological imprint. We leave this to a revision rather
than tune the existing 40-token budget.
(ii) The claim that only retrieval can deliver verbatim discriminating tokens is contingent on our model
sizes (124M / 1.1B). A 7-70B-scale model with stronger few-shot memorisation may emit discriminating
tokens parametrically; the R2 floor-of-zero observation should be re-tested at scale before treating
the “parametric encodes typology, retrieval encodes verbatim” separation as architecturally universal.
7.14

Valence oracle: oracle-conditioned protocol, not autonomous salience

The dual gate requires an external valence signal Vt (heuristic lexicon or DistilBERT-SST2 in our runs).
EVAF is therefore an oracle-conditioned consolidation protocol: it studies what happens when salience
and surprise jointly gate parametric writes, not how an agent autonomously infers personal significance.
The oracle-invariance study (§4.6) shows the four mechanistic signatures survive oracle swap — direction is
preserved, strength scales with oracle precision — but does not remove the oracle dependency. A self-contained
valence mechanism (learned from interaction feedback, multi-modal cues, or in-model affect heads) is out of
scope here and is the natural path to a deployable system.
7.15

Dynamical-freeze signature is operational, not a formal phase transition

We report a post-consolidation collapse of per-step prefix angles from a limit- cycle regime (≈ 17.7◦ mean step
angle, V5 baseline) to a near-fixed-point regime (< 10◦ on V9 events), with a pre/post z-score that separates
V9 from counterfactual ablations (§4.3). We call this an operational dynamical-freeze signature. We
do not claim a formal dynamical-systems phase transition: we do not establish a bifurcation parameter,
Lyapunov exponent crossing, or invariant manifold proof. The z-score is an empirical discriminator within
our protocol, not a theorem about attractor topology.
35


--- Page 36 ---
A related caveat concerns the sign of the consolidation term in EQ 3.2: it is a repulsion from the slow
EMA, not an attraction toward a memory anchor. The EMA-sign control in §4.3 shows this sign is necessary
for the observed limit-cycle→freeze signature, but a first-principles derivation from the coupled workingmemory/LoRA dynamics remains open.

These limitations are constraints on the scope of the present paper, not on the framework itself. We believe the
most decision-relevant of these — §7.2 (scaling-rule predictive power) and §7.7 (external persona benchmarks)
— are also the cleanest to close in a revision, and we welcome reviewer guidance on which to prioritise.

References
Yoram Burak and Ila R. Fiete. Accurate path integration in continuous attractor network models of grid
cells. PLoS Computational Biology, 5(2):e1000291, 2009. doi: 10.1371/journal.pcbi.1000291.
Arslan Chaudhry, Marcus Rohrbach, Mohamed Elhoseiny, Thalaiyasingam Ajanthan, Puneet K. Dokania,
Philip H. S. Torr, and Marc’Aurelio Ranzato. On tiny episodic memories in continual learning. In ICML
Workshop on Multi-Task and Lifelong Reinforcement Learning, 2019.
Alexis Chevalier, Alexander Wettig, Anirudh Ajith, and Danqi Chen. Adapting language models to compress
contexts. In EMNLP, 2023.
Rajas Chitale, Ankit Vaidya, Aditya Kane, and Archana Ghotkar. Task arithmetic with LoRA for continual
learning. arXiv preprint arXiv:2311.02428, 2023.
Payel Das, Subhajit Chaudhury, Elliot Nelson, Igor Melnyk, Sarath Swaminathan, Nico Daheim, Aurelie
Lozano, Brian Ross, Niloofar Tabaja, Akshay Gandhi, et al. Larimar: Large language models with episodic
memory control. In International Conference on Machine Learning (ICML), 2024.
Nicola De Cao, Wilker Aziz, and Ivan Titov. Editing factual knowledge in language models. In EMNLP,
2021.
Howard Eichenbaum. On the integration of space, time, and memory. Neuron, 95(5):1007–1018, 2017. doi:
10.1016/j.neuron.2017.06.036.
Nelson Elhage, Tristan Hume, Catherine Olsson, Nicholas Schiefer, Tom Henighan, Shauna Kravec, Zac
Hatfield-Dodds, Robert Lasenby, Dawn Drain, Carol Chen, et al. Toy models of superposition. Anthropic transformer-circuits.pub, 2022. URL https://transformer-circuits.pub/2022/toy_model/
index.html.
Robert M. French. Catastrophic forgetting in connectionist networks. Trends in Cognitive Sciences, 3(4):
128–135, 1999. doi: 10.1016/S1364-6613(99)01294-2.
Karl Friston. The free-energy principle: a unified brain theory? Nature Reviews Neuroscience, 11(2):127–138,
2010. doi: 10.1038/nrn2787.
Tao Ge, Jing Hu, Lei Wang, Xun Wang, Si-Qing Chen, and Furu Wei. In-context autoencoder for context
compression in a large language model. In International Conference on Learning Representations (ICLR),
2024.
Mor Geva, Avi Caciularu, Kevin Ro Wang, and Yoav Goldberg. Transformer feed-forward layers build
predictions by promoting concepts in the vocabulary space. In EMNLP, 2022.
Wes Gurnee and Max Tegmark. Language models represent space and time. In International Conference on
Learning Representations (ICLR), 2024.
Bernal Jiménez Gutiérrez, Yiheng Shu, Yu Gu, Michihiro Yasunaga, and Yu Su. HippoRAG: Neurobiologically
inspired long-term memory for large language models. arXiv preprint arXiv:2405.14831, 2024.
36


--- Page 37 ---
Thomas Hartvigsen, Swami Sankaranarayanan, Hamid Palangi, Yoon Kim, and Marzyeh Ghassemi. Aging
with GRACE: Lifelong model editing with discrete key-value adaptors. arXiv preprint arXiv:2211.11031,
2023.
Evan Hernandez, Arnab Sen Sharma, Tal Haklay, Kevin Meng, Martin Wattenberg, Jacob Andreas, Yonatan
Belinkov, and David Bau. Linearity of relation decoding in transformer language models. In International
Conference on Learning Representations (ICLR), 2024.
Edward J. Hu, Yelong Shen, Phillip Wallis, Zeyuan Allen-Zhu, Yuanzhi Li, Shean Wang, Lu Wang, and
Weizhu Chen. LoRA: Low-rank adaptation of large language models. In International Conference on
Learning Representations (ICLR), 2022. arXiv:2106.09685, 2021.
Gautier Izacard, Patrick Lewis, Maria Lomeli, Lucas Hosseini, Fabio Petroni, Timo Schick, Jane Dwivedi-Yu,
Armand Joulin, Sebastian Riedel, and Edouard Grave. Atlas: Few-shot learning with retrieval-augmented
language models. Journal of Machine Learning Research, 24(251):1–43, 2023.
Khurram Javed and Martha White. Meta-learning representations for continual learning. In Advances in
Neural Information Processing Systems (NeurIPS), 2019.
Ronald Kemker and Christopher Kanan. FearNet: Brain-inspired model for incremental learning. In
International Conference on Learning Representations (ICLR), 2018.
James Kirkpatrick, Razvan Pascanu, Neil Rabinowitz, Joel Veness, Guillaume Desjardins, Andrei A. Rusu,
Kieran Milan, John Quan, Tiago Ramalho, Agnieszka Grabska-Barwinska, et al. Overcoming catastrophic
forgetting in neural networks. Proceedings of the National Academy of Sciences, 114(13):3521–3526, 2017.
doi: 10.1073/pnas.1611835114.
Dharshan Kumaran, Demis Hassabis, and James L. McClelland. What learning systems do intelligent agents
need? complementary learning systems theory updated. Trends in Cognitive Sciences, 20(7):512–534, 2016.
doi: 10.1016/j.tics.2016.05.004.
Patrick Lewis, Ethan Perez, Aleksandra Piktus, Fabio Petroni, Vladimir Karpukhin, Naman Goyal, Heinrich
Küttler, Mike Lewis, Wen-tau Yih, Tim Rocktäschel, et al. Retrieval-augmented generation for knowledgeintensive NLP tasks. In Advances in Neural Information Processing Systems (NeurIPS), 2020.
Adyasha Maharana, Dong-Ho Lee, Sergey Tulyakov, Mohit Bansal, Francesco Barbieri, and Yuwei Fang. Evaluating very long-term conversational memory of LLM agents (LoCoMo). arXiv preprint arXiv:2402.17753,
2024.
Arun Mallya, Dillon Davis, and Svetlana Lazebnik. Piggyback: Adapting a single network to multiple tasks
by learning to mask weights. In European Conference on Computer Vision (ECCV), 2018.
James L. McClelland, Bruce L. McNaughton, and Randall C. O’Reilly. Why there are complementary learning
systems in the hippocampus and neocortex: Insights from the successes and failures of connectionist models
of learning and memory. Psychological Review, 102(3):419–457, 1995. doi: 10.1037/0033-295X.102.3.419.
Kevin Meng, David Bau, Alex Andonian, and Yonatan Belinkov. Locating and editing factual associations in
GPT. In Advances in Neural Information Processing Systems (NeurIPS), 2022.
Asier Mujika, Florian Meier, and Angelika Steger. Fast-slow recurrent neural networks. In Advances in
Neural Information Processing Systems (NeurIPS), 2017.
Charles Packer, Sarah Wooders, Kevin Lin, Vivian Fang, Shishir G. Patil, Ion Stoica, and Joseph E. Gonzalez.
MemGPT: Towards LLMs as operating systems. arXiv preprint arXiv:2310.08560, 2023.
Kiho Park, Yo Joong Choe, and Victor Veitch. The linear representation hypothesis and the geometry of
large language models. In International Conference on Machine Learning (ICML), 2024.
Preston Rasmussen, Pavlo Paliychuk, Travis Beauvais, Jack Ryan, and Daniel Chalef. Zep: A temporal
knowledge graph architecture for agent memory. arXiv preprint arXiv:2501.13956, 2025.
37


--- Page 38 ---
Anastasia Razdaibiedina, Yuning Mao, Rui Hou, Madian Khabsa, Mike Lewis, and Amjad Almahairi.
Progressive prompts: Continual learning for language models. In International Conference on Learning
Representations (ICLR), 2023.
David Rolnick, Arun Ahuja, Jonathan Schwarz, Timothy Lillicrap, and Greg Wayne. Experience replay for
continual learning. In Advances in Neural Information Processing Systems (NeurIPS), 2019.
Andrei A. Rusu, Neil C. Rabinowitz, Guillaume Desjardins, Hubert Soyer, James Kirkpatrick, Koray Kavukcuoglu, Razvan Pascanu, and Raia Hadsell. Progressive neural networks. arXiv preprint
arXiv:1606.04671, 2016.
Anna C. Schapiro, Nicholas B. Turk-Browne, Matthew M. Botvinick, and Kenneth A. Norman. Complementary
learning systems within the hippocampus: A neural network modelling approach. Philosophical Transactions
of the Royal Society B, 372(1711):20160049, 2017. doi: 10.1098/rstb.2016.0049.
Mohammad Tavakoli, Alireza Salemi, Carrie Ye, Mohamed Abdalla, Hamed Zamani, and J. Ross
Mitchell. Beyond a million tokens: Benchmarking and enhancing long-term memory in LLMs. arXiv
preprint arXiv:2510.27246, 2025. Introduces the BEAM long-context benchmark and the LIGHT
episodic/working/scratchpad memory framework.
Roman Vershynin. High-Dimensional Probability: An Introduction with Applications in Data Science.
Cambridge University Press, 2018.
Hugh R. Wilson and Jack D. Cowan. Excitatory and inhibitory interactions in localized populations of model
neurons. Biophysical Journal, 12(1):1–24, 1972. doi: 10.1016/S0006-3495(72)86068-5.
Di Wu, Hongwei Wang, Wenhao Yu, Yunsheng Zhang, Kai-Wei Chang, and Dong Yu. LongMemEval:
Benchmarking chat assistants on long-term interactive memory. arXiv preprint arXiv:2410.10813, 2024.
Yuhuai Wu, Markus N. Rabe, DeLesley Hutchins, and Christian Szegedy. Memorizing transformers. In
International Conference on Learning Representations (ICLR), 2022.
Friedemann Zenke, Ben Poole, and Surya Ganguli. Continual learning through synaptic intelligence. In
International Conference on Machine Learning (ICML), 2017.
Saizheng Zhang, Emily Dinan, Jack Urbanek, Arthur Szlam, Douwe Kiela, and Jason Weston. Personalizing
dialogue agents: I have a dog, do you have pets too? In ACL, 2018.

A

Implementation details and hyperparameters

All experiments use the v9.0-frozen configuration. The model-specific triple (τs , λreg , lrLoRA ) follows the
re-anchoring rules of §5 and is reported in §4.1; every remaining hyperparameter is model-invariant and
is listed in Table 16. LoRA adapters are attached to the attention projections (q/k/v/o, or the Conv1D
analogues for GPT-2) of the otherwise-frozen base model. Algorithm 1 gives the end-to-end stream loop,
including the dual gate, the on-trigger inner loop with experience replay and the EWC-style L2 anchor, and
the Test–Retest instrumentation.

38


--- Page 39 ---
Table 16: Model-invariant hyperparameters (v9.0-frozen). The per-model (τs , λreg , lrLoRA ) values are given
in §4.1; the base model’s hidden width D is model-dependent.
Symbol Meaning
Value
P
r
Ninner
|B|
K
τwrite
α
k
kv
τv
ρ
λcon

prefix length (working-memory tokens)
LoRA rank
inner-loop AdamW steps per consolidation event
polarized-buffer capacity (trigger size)
experience-replay samples per event
dual-gate admission threshold
prefix Euler step size
surprise-gate sharpness
valence-gate sharpness
valence threshold
base EMA rate (gate-modulated to ρt )
repulsive-EMA (contrastive inertia) coefficient

Algorithm 1 EVAF stream loop (one pass over the interaction stream).

10
32
5
4
4
0.55
1.0
1.0
10.0
0.5
0.1
2.0

√
Require: stream {yt }; frozen base model; prefix s(0) projected onto (S D−1 ( D))P ; LoRA θ ← θ0 ; EMA
state sEMA ; buffer B ← ∅; valence oracle V (·)
1: for each step t in the stream do
2:
St ← epistemic surprise of yt under prefix s(t)
▷ Eq. 3.1

3:
s(t+1) ← Π(S D−1 (√D))P s(t) + α[−∇s St + λcon (s(t) − sEMA (t))]
▷ Eq. 3.2


4:
ρt ← ρ σ k(St − τs ) ; sEMA (t+1) ← Π(S D−1 (√D))P (1 − ρt ) sEMA (t) + ρt s(t+1) ▷ gate-modulated
EMA
5:
Vt ← V (yt )


6:
if σ kv (Vt − τv ) · σ k(St − τs ) > τwrite then
7:
B ← B ∪ {yt }
▷ dual gate, Eq. 3.4
8:
end if
9:
if |B| = 4 then
▷ consolidation event at step t∗
10:
R ← sample K items uniformly from the past stream \ B
11:
θpre ← θ
12:
for i = 1 to Ninner do
P
1
∗
2
13:
θ ← AdamW step on |B∪R|
▷ Eq. 3.3
y∈B∪R CE(y | s(t ), θ) + λreg ∥θ − θ0 ∥2
14:
end for
15:
θpost ← θ
16:
record Test–Retest signatures ∆S B , ∆S R , ∆S global and freeze z
▷ Eq. 3.5
17:
B←∅
18:
end if
19: end for

39


--- Page 40 ---
B

Persona corpus and characteristic-keyword set

Table 17: Per-persona discriminating attributes (the only persona-unique keywords; used for the selectivity /
cross-persona contamination test).
Persona

Language

City

Instrument

Alice
Bob
Carol
David
Eve

Mandarin
Portuguese
Tamil
Swahili
Finnish

Reykjavik
Marrakech
Helsinki
Quito
Tashkent

cello
oboe
violin
clarinet
harp

The downstream persona case study (§4.10–§4.11) uses five personas, {Alice, Bob, Carol, David, Eve}, that
share a common biographical fact schema and differ only in three discriminating attributes (spoken language,
home city, musical instrument). Productive-recall hit rate (§4.11) counts case-insensitive substring matches
of the shared persona-fact keyword set:
climb, Dolomite, via-ferrata, ferrata, seven, routes, fluent, poem, literary,
journal, bakery, Maple, morning, four thirty, Carnegie, recital, spring, vintage,
chess, allergic, peanut, pollen, epinephrine

together with each persona’s three discriminating tokens (Table 17). The cross-persona contamination /
selectivity metric (§4.11) uses only the discriminating tokens, since these are the only persona-unique signals.

40

